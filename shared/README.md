# Shared

This top-level folder is reserved for cross-cutting assets that are not packaged as runtime code,
such as product schemas, design tokens, policy documents, and integration contracts.

Runtime TypeScript shared code lives in `packages/shared` so it can be versioned and consumed by
workspace applications through normal package boundaries.
