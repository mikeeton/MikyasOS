generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserStatus {
  PENDING
  ACTIVE
  SUSPENDED
  DELETED
}

enum InvitationStatus {
  PENDING
  ACCEPTED
  REVOKED
  EXPIRED
}

enum OrganisationRoleType {
  OWNER
  ADMIN
  MEMBER
  GUEST
  CUSTOM
}

enum CompanyStatus {
  PROSPECT
  ACTIVE
  INACTIVE
  CUSTOMER
  PARTNER
  SUPPLIER
  ARCHIVED
}

enum LeadStatus {
  NEW
  CONTACTED
  QUALIFIED
  UNQUALIFIED
  CONVERTED
  LOST
}

enum OpportunityStage {
  DISCOVERY
  QUALIFICATION
  PROPOSAL
  NEGOTIATION
  WON
  LOST
}

enum OpportunityStatus {
  OPEN
  WON
  LOST
  ARCHIVED
}

enum CustomerActivityType {
  COMPANY_CREATED
  COMPANY_UPDATED
  CONTACT_ADDED
  LEAD_CREATED
  LEAD_UPDATED
  LEAD_CONVERTED
  OPPORTUNITY_CREATED
  OPPORTUNITY_UPDATED
  OPPORTUNITY_WON
  OPPORTUNITY_LOST
  NOTE_ADDED
  FILE_UPLOADED
  TAG_CHANGED
  CUSTOMER_EDITED
}

enum ProjectStatus {
  PLANNED
  ACTIVE
  ON_HOLD
  COMPLETED
  ARCHIVED
}

enum ProjectPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum TaskStatus {
  TODO
  IN_PROGRESS
  IN_REVIEW
  BLOCKED
  DONE
  CANCELLED
}

enum TaskPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum MilestoneStatus {
  PLANNED
  ACTIVE
  COMPLETED
  MISSED
}

enum ProjectActivityType {
  PROJECT_CREATED
  PROJECT_UPDATED
  PROJECT_ARCHIVED
  PROJECT_RESTORED
  TASK_CREATED
  TASK_UPDATED
  TASK_MOVED
  TASK_ASSIGNED
  TASK_COMPLETED
  COMMENT_ADDED
  FILE_UPLOADED
  MILESTONE_UPDATED
  STATUS_CHANGED
  LABEL_CHANGED
  TIME_TRACKED
}

enum DocumentType {
  PDF
  DOCX
  XLSX
  CSV
  IMAGE
  TEXT
  MARKDOWN
  OTHER
}

enum DocumentStatus {
  ACTIVE
  PROCESSING
  ARCHIVED
  DELETED
}

enum DocumentVisibility {
  PRIVATE
  ORGANISATION
  RESTRICTED
}

enum DocumentPermissionLevel {
  VIEW
  COMMENT
  EDIT
  MANAGE
  OWNER
}

enum DocumentActivityAction {
  DOCUMENT_UPLOADED
  DOCUMENT_VIEWED
  DOCUMENT_DOWNLOADED
  DOCUMENT_EDITED
  DOCUMENT_RENAMED
  DOCUMENT_MOVED
  DOCUMENT_DELETED
  DOCUMENT_RESTORED
  VERSION_UPLOADED
  VERSION_RESTORED
  PERMISSION_CHANGED
  FOLDER_CREATED
  FOLDER_RENAMED
  FOLDER_MOVED
  FOLDER_DELETED
  TAG_ADDED
  TAG_REMOVED
  DOCUMENT_LINKED
  DOCUMENT_UNLINKED
}

enum DocumentLinkEntityType {
  COMPANY
  CONTACT
  PROJECT
  TASK
  INVOICE
  MEETING
  AUTOMATION
}

enum ConversationType {
  DIRECT
  GROUP
  CHANNEL
  ANNOUNCEMENT
}

enum ConversationVisibility {
  PUBLIC
  PRIVATE
  DEPARTMENT
  PROJECT
  COMPANY_WIDE
}

enum ConversationMemberRole {
  OWNER
  ADMIN
  MEMBER
  GUEST
}

enum MessageStatus {
  ACTIVE
  EDITED
  DELETED
}

enum MessageActivityAction {
  SENT
  EDITED
  DELETED
  REACTION_ADDED
  REACTION_REMOVED
  READ
  ATTACHMENT_ADDED
  THREAD_STARTED
}

enum AnnouncementPriority {
  LOW
  NORMAL
  HIGH
  URGENT
}

enum MeetingStatus {
  DRAFT
  SCHEDULED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum MeetingParticipantStatus {
  INVITED
  ACCEPTED
  DECLINED
  TENTATIVE
}

enum PresenceStatus {
  ONLINE
  AWAY
  BUSY
  OFFLINE
  IN_MEETING
  WORKING_ON_PROJECT
}

enum WorkflowStatus {
  DRAFT
  ACTIVE
  PAUSED
  ARCHIVED
}

enum WorkflowTriggerType {
  CUSTOMER_CREATED
  LEAD_WON
  PROJECT_CREATED
  TASK_COMPLETED
  DOCUMENT_UPLOADED
  MEETING_ENDED
  INVOICE_PAID
  CALENDAR_EVENT
  WEBHOOK_RECEIVED
  MANUAL
  SCHEDULED
}

enum WorkflowConditionOperator {
  EQUALS
  CONTAINS
  GREATER_THAN
  LESS_THAN
  DATE
  USER
  ROLE
  DEPARTMENT
  PROJECT_STATUS
  CRM_STATUS
  DOCUMENT_TAG
  CUSTOM_EXPRESSION
}

enum WorkflowActionType {
  CREATE_TASK
  ASSIGN_TASK
  CREATE_PROJECT
  CREATE_COMPANY
  CREATE_CONTACT
  SEND_NOTIFICATION
  SEND_EMAIL
  GENERATE_AI_SUMMARY
  CREATE_MEETING
  CREATE_CALENDAR_EVENT
  MOVE_CRM_STAGE
  UPDATE_RECORD
  CREATE_DOCUMENT
  RUN_WEBHOOK
  WAIT
  APPROVAL
}

enum WorkflowExecutionStatus {
  QUEUED
  RUNNING
  SUCCEEDED
  FAILED
  CANCELLED
  WAITING_APPROVAL
}

enum WorkflowScheduleType {
  ONCE
  HOURLY
  DAILY
  WEEKLY
  MONTHLY
  CRON
}

enum WorkflowApprovalStatus {
  PENDING
  APPROVED
  REJECTED
  CANCELLED
}

enum WorkflowLogSeverity {
  DEBUG
  INFO
  WARN
  ERROR
}

enum FinanceDocumentStatus {
  DRAFT
  SENT
  APPROVED
  PARTIALLY_PAID
  PAID
  OVERDUE
  VOID
  CANCELLED
}

enum QuoteStatus {
  DRAFT
  SENT
  ACCEPTED
  REJECTED
  EXPIRED
  CONVERTED
}

enum ExpenseStatus {
  DRAFT
  SUBMITTED
  APPROVED
  REJECTED
  PAID
}

enum PaymentMethod {
  BANK_TRANSFER
  CARD
  CASH
  STRIPE
  PAYPAL
  MANUAL
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
  REFUNDED
  CANCELLED
}

enum PurchaseOrderStatus {
  DRAFT
  SUBMITTED
  APPROVED
  ORDERED
  PARTIALLY_RECEIVED
  RECEIVED
  CANCELLED
}

enum RecurringBillingFrequency {
  WEEKLY
  MONTHLY
  QUARTERLY
  YEARLY
}

enum SubscriptionStatus {
  TRIALING
  ACTIVE
  PAUSED
  CANCELLED
  EXPIRED
}

enum TransactionType {
  INCOME
  EXPENSE
  TRANSFER
  ADJUSTMENT
}

enum AccountType {
  BANK
  CASH
  CREDIT_CARD
  REVENUE
  EXPENSE
  ASSET
  LIABILITY
  EQUITY
}

enum FinancialReportType {
  PROFIT_AND_LOSS
  REVENUE
  EXPENSES
  CASH_FLOW
  TAX_SUMMARY
  CUSTOMER_REVENUE
  PROJECT_PROFITABILITY
  DEPARTMENT_SPEND
}

enum CashFlowDirection {
  INFLOW
  OUTFLOW
}

enum DashboardVisibility {
  PRIVATE
  TEAM
  ORGANISATION
}

enum WidgetType {
  KPI
  CHART
  TABLE
  FUNNEL
  GAUGE
  SCORECARD
  ACTIVITY
  AI_BRIEFING
}

enum MetricType {
  REVENUE
  EXPENSES
  PROFIT
  CASH_FLOW
  CUSTOMER_GROWTH
  LEAD_CONVERSION
  SALES_PIPELINE
  PROJECT_COMPLETION
  TASK_VELOCITY
  DOCUMENT_ACTIVITY
  AUTOMATION_USAGE
  EMPLOYEE_PRODUCTIVITY
  MEETING_ACTIVITY
  AI_USAGE
}

enum AnalyticsReportType {
  REVENUE
  EXPENSE
  SALES
  PROJECT
  CRM
  EMPLOYEE
  AUTOMATION
  AI_USAGE
  EXECUTIVE_SUMMARY
}

enum ChartType {
  LINE
  BAR
  AREA
  PIE
  SCATTER
  HEATMAP
  TREEMAP
  GAUGE
  FUNNEL
  RADAR
}

enum ForecastType {
  REVENUE
  CASH_FLOW
  SALES
  PROJECT_DELIVERY
  RESOURCE
}

enum AnalyticsEventType {
  DASHBOARD_VIEWED
  WIDGET_CREATED
  REPORT_GENERATED
  KPI_CREATED
  FORECAST_CREATED
  SNAPSHOT_CREATED
  FILTER_APPLIED
  SAVED_VIEW_CREATED
}

enum IntegrationCategory {
  CLOUD_SERVICE
  COMMUNICATION
  ACCOUNTING
  STORAGE
  DEVELOPMENT
  MARKETING
  CRM
  PAYMENTS
  CALENDAR
  CUSTOM
}

enum IntegrationAuthType {
  OAUTH2
  API_KEY
  BEARER_TOKEN
  BASIC
  WEBHOOK_SECRET
  SAML
}

enum IntegrationStatus {
  AVAILABLE
  INSTALLED
  CONNECTED
  DISCONNECTED
  ERROR
  ARCHIVED
}

enum ConnectionStatus {
  PENDING
  CONNECTED
  DISCONNECTED
  ERROR
  EXPIRED
}

enum SyncStatus {
  QUEUED
  RUNNING
  COMPLETED
  FAILED
  CANCELLED
}

enum SyncMode {
  MANUAL
  SCHEDULED
  WEBHOOK
  INCREMENTAL
  FULL
}

enum WebhookDirection {
  INCOMING
  OUTGOING
}

enum WebhookDeliveryStatus {
  PENDING
  DELIVERED
  FAILED
  RETRYING
}

enum IntegrationLogSeverity {
  DEBUG
  INFO
  WARN
  ERROR
}

enum IntegrationHealthStatus {
  HEALTHY
  DEGRADED
  DOWN
  UNKNOWN
}

enum IntegrationEventType {
  INSTALLED
  CONNECTED
  DISCONNECTED
  CREDENTIAL_ROTATED
  SYNC_STARTED
  SYNC_COMPLETED
  SYNC_FAILED
  WEBHOOK_RECEIVED
  WEBHOOK_SENT
}

enum EnterprisePolicyType {
  SECURITY
  ACCESS
  DATA_RETENTION
  COMPLIANCE
  AI_GOVERNANCE
  ADMINISTRATION
}

enum PolicyStatus {
  DRAFT
  ACTIVE
  DISABLED
  ARCHIVED
}

enum AuditSeverity {
  INFO
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum ComplianceFramework {
  GDPR
  SOC2
  ISO27001
  HIPAA
  PCIDSS
}

enum ComplianceStatus {
  READY
  IN_PROGRESS
  GAP
  NOT_APPLICABLE
}

enum SSOProviderType {
  SAML2
  OPENID_CONNECT
  OAUTH_ENTERPRISE
  MICROSOFT_ENTRA
  OKTA
  GOOGLE_WORKSPACE
  PING_IDENTITY
  ONELOGIN
}

enum DirectoryProviderType {
  SCIM
  ACTIVE_DIRECTORY
  LDAP
  GOOGLE_DIRECTORY
  MICROSOFT_GRAPH
}

enum RetentionAction {
  RETAIN
  ARCHIVE
  DELETE
  LEGAL_HOLD
}

enum LicenseStatus {
  TRIAL
  ACTIVE
  SUSPENDED
  EXPIRED
  CANCELLED
}

enum PlatformStatus {
  OPERATIONAL
  DEGRADED
  PARTIAL_OUTAGE
  MAJOR_OUTAGE
  MAINTENANCE
}

enum IncidentSeverity {
  SEV1
  SEV2
  SEV3
  SEV4
}

enum IncidentStatus {
  INVESTIGATING
  IDENTIFIED
  MONITORING
  RESOLVED
  POSTMORTEM_PENDING
  CLOSED
}

enum DeploymentStatus {
  PENDING
  RUNNING
  SUCCEEDED
  FAILED
  ROLLED_BACK
}

enum FeatureFlagScope {
  GLOBAL
  ORGANISATION
  USER
  ENVIRONMENT
}

enum CircuitBreakerState {
  CLOSED
  OPEN
  HALF_OPEN
}

enum BackupStatus {
  SCHEDULED
  RUNNING
  COMPLETED
  FAILED
  VERIFIED
}

model User {
  id                        String                   @id @default(uuid()) @db.Uuid
  email                     String                   @unique
  name                      String
  passwordHash              String
  status                    UserStatus               @default(ACTIVE)
  activeOrganisationId      String?                  @db.Uuid
  lastLoginAt               DateTime?
  createdAt                 DateTime                 @default(now())
  updatedAt                 DateTime                 @updatedAt
  deletedAt                 DateTime?
  memberships               OrganisationMember[]
  ownedOrganisations        Organisation[]           @relation("OrganisationOwner")
  invitationsSent           Invitation[]             @relation("InvitationInviter")
  invitationsAccepted       Invitation[]             @relation("InvitationAcceptedBy")
  sessions                  Session[]
  refreshTokens             RefreshToken[]
  auditLogs                 AuditLog[]
  assignedLeads             Lead[]                   @relation("LeadAssignee")
  ownedOpportunities        Opportunity[]            @relation("OpportunityOwner")
  customerNotes             CustomerNote[]           @relation("CustomerNoteAuthor")
  customerFiles             CustomerFile[]           @relation("CustomerFileUploader")
  customerActivities        CustomerActivity[]       @relation("CustomerActivityActor")
  ownedProjects             Project[]                @relation("ProjectOwner")
  assignedTasks             Task[]                   @relation("TaskAssignee")
  reportedTasks             Task[]                   @relation("TaskReporter")
  projectComments           ProjectComment[]         @relation("ProjectCommentAuthor")
  projectFiles              ProjectFile[]            @relation("ProjectFileUploader")
  timeEntries               TimeEntry[]              @relation("TimeEntryUser")
  projectActivities         ProjectActivity[]        @relation("ProjectActivityActor")
  ownedFolders              Folder[]                 @relation("FolderOwner")
  ownedDocuments            Document[]               @relation("DocumentOwner")
  documentVersions          DocumentVersion[]        @relation("DocumentVersionUploader")
  documentPermissions       DocumentPermission[]     @relation("DocumentPermissionUser")
  documentActivities        DocumentActivity[]       @relation("DocumentActivityActor")
  documentShares            DocumentShare[]          @relation("DocumentShareCreator")
  createdConversations      Conversation[]           @relation("ConversationCreator")
  conversationMembers       ConversationMember[]
  messages                  Message[]                @relation("MessageAuthor")
  messageAttachments        MessageAttachment[]      @relation("MessageAttachmentUploader")
  messageReactions          MessageReaction[]
  messageReadReceipts       MessageReadReceipt[]
  threads                   Thread[]                 @relation("ThreadCreator")
  announcements             Announcement[]           @relation("AnnouncementAuthor")
  meetings                  Meeting[]                @relation("MeetingOrganizer")
  meetingParticipants       MeetingParticipant[]
  meetingNotes              MeetingNote[]            @relation("MeetingNoteAuthor")
  messageActivities         MessageActivity[]        @relation("MessageActivityActor")
  notificationPrefs         NotificationPreference[]
  workflows                 Workflow[]               @relation("WorkflowCreator")
  workflowExecutions        WorkflowExecution[]      @relation("WorkflowExecutionActor")
  workflowApprovals         WorkflowApproval[]       @relation("WorkflowApprovalRequester")
  workflowApprovalsReviewed WorkflowApproval[]       @relation("WorkflowApprovalReviewer")
  workflowAudits            WorkflowAudit[]          @relation("WorkflowAuditActor")
  financeAudits             FinanceAudit[]           @relation("FinanceAuditActor")
  activeOrganisation        Organisation?            @relation("ActiveOrganisation", fields: [activeOrganisationId], references: [id], onDelete: SetNull)

  @@index([status])
  @@index([activeOrganisationId])
  @@index([deletedAt])
}

model Organisation {
  id                           String                   @id @default(uuid()) @db.Uuid
  name                         String
  slug                         String                   @unique
  industry                     String?
  companySize                  String?
  country                      String?
  timezone                     String?
  currency                     String?
  ownerId                      String                   @db.Uuid
  createdAt                    DateTime                 @default(now())
  updatedAt                    DateTime                 @updatedAt
  deletedAt                    DateTime?
  owner                        User                     @relation("OrganisationOwner", fields: [ownerId], references: [id], onDelete: Restrict)
  activeUsers                  User[]                   @relation("ActiveOrganisation")
  members                      OrganisationMember[]
  roles                        Role[]
  invitations                  Invitation[]
  sessions                     Session[]
  refreshTokens                RefreshToken[]
  auditLogs                    AuditLog[]
  companies                    Company[]
  contacts                     Contact[]
  leads                        Lead[]
  opportunities                Opportunity[]
  customerNotes                CustomerNote[]
  customerFiles                CustomerFile[]
  customerTags                 CustomerTag[]
  activities                   CustomerActivity[]
  projects                     Project[]
  tasks                        Task[]
  projectLabels                ProjectLabel[]
  projectFiles                 ProjectFile[]
  timeEntries                  TimeEntry[]
  projectBoards                ProjectBoard[]
  projectActivities            ProjectActivity[]
  projectComments              ProjectComment[]
  folders                      Folder[]
  documents                    Document[]
  documentVersions             DocumentVersion[]
  documentPermissions          DocumentPermission[]
  documentActivities           DocumentActivity[]
  documentTags                 DocumentTag[]
  documentLinks                DocumentLink[]
  documentShares               DocumentShare[]
  conversations                Conversation[]
  conversationMembers          ConversationMember[]
  messages                     Message[]
  messageAttachments           MessageAttachment[]
  messageReactions             MessageReaction[]
  messageReadReceipts          MessageReadReceipt[]
  threads                      Thread[]
  announcements                Announcement[]
  meetings                     Meeting[]
  meetingParticipants          MeetingParticipant[]
  meetingNotes                 MeetingNote[]
  meetingRecordings            MeetingRecording[]
  messageActivities            MessageActivity[]
  notificationPrefs            NotificationPreference[]
  workflows                    Workflow[]
  workflowVersions             WorkflowVersion[]
  workflowExecutions           WorkflowExecution[]
  workflowTriggers             WorkflowTrigger[]
  workflowConditions           WorkflowCondition[]
  workflowActions              WorkflowAction[]
  workflowVariables            WorkflowVariable[]
  workflowTemplates            WorkflowTemplate[]
  workflowSchedules            WorkflowSchedule[]
  workflowApprovals            WorkflowApproval[]
  workflowLogs                 WorkflowLog[]
  workflowAudits               WorkflowAudit[]
  workflowErrors               WorkflowError[]
  invoices                     Invoice[]
  invoiceItems                 InvoiceItem[]
  quotes                       Quote[]
  quoteItems                   QuoteItem[]
  expenses                     Expense[]
  expenseCategories            ExpenseCategory[]
  payments                     Payment[]
  purchaseOrders               PurchaseOrder[]
  purchaseOrderItems           PurchaseOrderItem[]
  recurringInvoices            RecurringInvoice[]
  subscriptions                Subscription[]
  budgets                      Budget[]
  transactions                 Transaction[]
  accounts                     Account[]
  taxRates                     TaxRate[]
  currencies                   Currency[]
  financialReports             FinancialReport[]
  cashFlowEntries              CashFlowEntry[]
  journalEntries               JournalEntry[]
  financeAudits                FinanceAudit[]
  dashboards                   Dashboard[]
  dashboardWidgets             DashboardWidget[]
  metrics                      Metric[]
  metricValues                 MetricValue[]
  kpis                         KPI[]
  analyticsReports             Report[]
  reportSchedules              ReportSchedule[]
  dataSources                  DataSource[]
  chartConfigurations          ChartConfiguration[]
  forecasts                    Forecast[]
  businessSnapshots            BusinessSnapshot[]
  analyticsEvents              AnalyticsEvent[]
  analyticsFilters             AnalyticsFilter[]
  savedViews                   SavedView[]
  integrations                 Integration[]
  integrationTypes             IntegrationType[]
  integrationCredentials       IntegrationCredential[]
  integrationConnections       IntegrationConnection[]
  integrationSyncs             IntegrationSync[]
  webhookEndpoints             WebhookEndpoint[]
  webhookDeliveries            WebhookDelivery[]
  connectors                   Connector[]
  connectorActions             ConnectorAction[]
  syncLogs                     SyncLog[]
  integrationHealth            IntegrationHealth[]
  integrationEvents            IntegrationEvent[]
  apiKeys                      APIKey[]
  businessUnits                BusinessUnit[]
  hierarchyRecords             OrganisationHierarchy[]  @relation("HierarchyOwner")
  organisationHierarchies      OrganisationHierarchy[]  @relation("HierarchyParent")
  childOrganisationHierarchies OrganisationHierarchy[]  @relation("HierarchyChild")
  enterprisePolicies           EnterprisePolicy[]
  auditEvents                  AuditEvent[]
  securityPolicies             SecurityPolicy[]
  customRoles                  CustomRole[]
  permissionGroups             PermissionGroup[]
  ssoProviders                 SSOProvider[]
  directoryConnections         DirectoryConnection[]
  complianceRecords            ComplianceRecord[]
  retentionPolicies            RetentionPolicy[]
  legalHolds                   LegalHold[]
  licenses                     License[]
  platformHealthChecks         PlatformHealthCheck[]
  platformMetrics              PlatformMetric[]
  incidents                    Incident[]
  incidentTimeline             IncidentTimeline[]
  deploymentRecords            DeploymentRecord[]
  featureFlags                 FeatureFlag[]
  backupRecords                BackupRecord[]
  recoveryActions              RecoveryAction[]
  circuitBreakers              CircuitBreaker[]
  jobQueueSnapshots            JobQueueSnapshot[]
  costRecords                  CostRecord[]

  @@index([ownerId])
  @@index([deletedAt])
}

model OrganisationMember {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  userId         String               @db.Uuid
  roleId         String               @db.Uuid
  roleType       OrganisationRoleType @default(MEMBER)
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  user           User                 @relation(fields: [userId], references: [id], onDelete: Cascade)
  role           Role                 @relation(fields: [roleId], references: [id], onDelete: Restrict)

  @@unique([organisationId, userId])
  @@index([userId])
  @@index([roleId])
  @@index([organisationId, roleType])
  @@index([deletedAt])
}

model Role {
  id                  String               @id @default(uuid()) @db.Uuid
  organisationId      String               @db.Uuid
  name                String
  description         String?
  type                OrganisationRoleType @default(CUSTOM)
  isSystem            Boolean              @default(false)
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt
  deletedAt           DateTime?
  organisation        Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  members             OrganisationMember[]
  rolePermissions     RolePermission[]
  invitations         Invitation[]
  documentPermissions DocumentPermission[]

  @@unique([organisationId, name])
  @@index([organisationId, type])
  @@index([deletedAt])
}

model Permission {
  id              String           @id @default(uuid()) @db.Uuid
  key             String           @unique
  description     String?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  rolePermissions RolePermission[]
}

model RolePermission {
  id           String     @id @default(uuid()) @db.Uuid
  roleId       String     @db.Uuid
  permissionId String     @db.Uuid
  createdAt    DateTime   @default(now())
  role         Role       @relation(fields: [roleId], references: [id], onDelete: Cascade)
  permission   Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)

  @@unique([roleId, permissionId])
  @@index([permissionId])
}

model Invitation {
  id             String           @id @default(uuid()) @db.Uuid
  organisationId String           @db.Uuid
  email          String
  roleId         String           @db.Uuid
  tokenHash      String           @unique
  status         InvitationStatus @default(PENDING)
  invitedById    String           @db.Uuid
  acceptedById   String?          @db.Uuid
  expiresAt      DateTime
  acceptedAt     DateTime?
  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt
  revokedAt      DateTime?
  organisation   Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  role           Role             @relation(fields: [roleId], references: [id], onDelete: Restrict)
  invitedBy      User             @relation("InvitationInviter", fields: [invitedById], references: [id], onDelete: Restrict)
  acceptedBy     User?            @relation("InvitationAcceptedBy", fields: [acceptedById], references: [id], onDelete: SetNull)

  @@unique([organisationId, email, status])
  @@index([organisationId])
  @@index([email])
  @@index([expiresAt])
}

model Session {
  id             String         @id @default(uuid()) @db.Uuid
  userId         String         @db.Uuid
  organisationId String?        @db.Uuid
  userAgent      String?
  ipAddress      String?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
  expiresAt      DateTime
  revokedAt      DateTime?
  user           User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  organisation   Organisation?  @relation(fields: [organisationId], references: [id], onDelete: SetNull)
  refreshTokens  RefreshToken[]

  @@index([userId])
  @@index([organisationId])
  @@index([expiresAt])
  @@index([revokedAt])
}

model RefreshToken {
  id             String        @id @default(uuid()) @db.Uuid
  userId         String        @db.Uuid
  organisationId String?       @db.Uuid
  sessionId      String        @db.Uuid
  tokenHash      String        @unique
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  expiresAt      DateTime
  revokedAt      DateTime?
  replacedById   String?       @unique @db.Uuid
  user           User          @relation(fields: [userId], references: [id], onDelete: Cascade)
  organisation   Organisation? @relation(fields: [organisationId], references: [id], onDelete: SetNull)
  session        Session       @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  replacedBy     RefreshToken? @relation("RefreshTokenRotation", fields: [replacedById], references: [id], onDelete: SetNull)
  replaces       RefreshToken? @relation("RefreshTokenRotation")

  @@index([userId])
  @@index([sessionId])
  @@index([organisationId])
  @@index([expiresAt])
  @@index([revokedAt])
}

model AuditLog {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String?       @db.Uuid
  actorUserId    String?       @db.Uuid
  action         String
  entityType     String
  entityId       String?
  metadata       Json?
  ipAddress      String?
  userAgent      String?
  createdAt      DateTime      @default(now())
  organisation   Organisation? @relation(fields: [organisationId], references: [id], onDelete: SetNull)
  actorUser      User?         @relation(fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, createdAt])
  @@index([actorUserId])
  @@index([action])
}

model Company {
  id              String             @id @default(uuid()) @db.Uuid
  organisationId  String             @db.Uuid
  name            String
  legalName       String?
  website         String?
  email           String?
  phone           String?
  industry        String?
  companySize     String?
  annualRevenue   Decimal?           @db.Decimal(14, 2)
  country         String?
  city            String?
  address         String?
  postcode        String?
  linkedin        String?
  status          CompanyStatus      @default(PROSPECT)
  logo            String?
  createdAt       DateTime           @default(now())
  updatedAt       DateTime           @updatedAt
  deletedAt       DateTime?
  organisation    Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  contacts        Contact[]
  leads           Lead[]
  opportunities   Opportunity[]
  notes           CustomerNote[]
  files           CustomerFile[]
  activities      CustomerActivity[]
  tags            CompanyTag[]
  projects        Project[]
  meetings        Meeting[]
  meetingNotes    MeetingNote[]
  invoices        Invoice[]
  quotes          Quote[]
  expenses        Expense[]
  payments        Payment[]
  purchaseOrders  PurchaseOrder[]
  subscriptions   Subscription[]
  transactions    Transaction[]
  cashFlowEntries CashFlowEntry[]

  @@index([organisationId, name])
  @@index([organisationId, status])
  @@index([organisationId, industry])
  @@index([organisationId, country])
  @@index([organisationId, city])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model Contact {
  id             String             @id @default(uuid()) @db.Uuid
  organisationId String             @db.Uuid
  companyId      String?            @db.Uuid
  firstName      String
  lastName       String
  email          String?
  phone          String?
  mobile         String?
  jobTitle       String?
  department     String?
  linkedin       String?
  avatar         String?
  birthday       DateTime?
  notes          String?
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt
  deletedAt      DateTime?
  organisation   Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  company        Company?           @relation(fields: [companyId], references: [id], onDelete: SetNull)
  activities     CustomerActivity[]
  customerNotes  CustomerNote[]
  files          CustomerFile[]

  @@index([organisationId, companyId])
  @@index([organisationId, email])
  @@index([organisationId, firstName, lastName])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model Lead {
  id                String             @id @default(uuid()) @db.Uuid
  organisationId    String             @db.Uuid
  companyId         String?            @db.Uuid
  assignedTo        String?            @db.Uuid
  source            String?
  status            LeadStatus         @default(NEW)
  probability       Int                @default(0)
  estimatedValue    Decimal?           @db.Decimal(14, 2)
  expectedCloseDate DateTime?
  description       String?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  deletedAt         DateTime?
  organisation      Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  company           Company?           @relation(fields: [companyId], references: [id], onDelete: SetNull)
  assignee          User?              @relation("LeadAssignee", fields: [assignedTo], references: [id], onDelete: SetNull)
  activities        CustomerActivity[]

  @@index([organisationId, companyId])
  @@index([organisationId, assignedTo])
  @@index([organisationId, status])
  @@index([organisationId, expectedCloseDate])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model Opportunity {
  id                String             @id @default(uuid()) @db.Uuid
  organisationId    String             @db.Uuid
  companyId         String?            @db.Uuid
  owner             String?            @db.Uuid
  stage             OpportunityStage   @default(DISCOVERY)
  probability       Int                @default(0)
  estimatedRevenue  Decimal?           @db.Decimal(14, 2)
  expectedCloseDate DateTime?
  status            OpportunityStatus  @default(OPEN)
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  deletedAt         DateTime?
  organisation      Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  company           Company?           @relation(fields: [companyId], references: [id], onDelete: SetNull)
  ownerUser         User?              @relation("OpportunityOwner", fields: [owner], references: [id], onDelete: SetNull)
  activities        CustomerActivity[]

  @@index([organisationId, companyId])
  @@index([organisationId, owner])
  @@index([organisationId, stage])
  @@index([organisationId, status])
  @@index([organisationId, expectedCloseDate])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model CustomerNote {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  companyId      String?      @db.Uuid
  contactId      String?      @db.Uuid
  authorId       String?      @db.Uuid
  content        String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  company        Company?     @relation(fields: [companyId], references: [id], onDelete: Cascade)
  contact        Contact?     @relation(fields: [contactId], references: [id], onDelete: Cascade)
  author         User?        @relation("CustomerNoteAuthor", fields: [authorId], references: [id], onDelete: SetNull)

  @@index([organisationId, companyId])
  @@index([organisationId, contactId])
  @@index([organisationId, authorId])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model CustomerFile {
  id               String       @id @default(uuid()) @db.Uuid
  organisationId   String       @db.Uuid
  companyId        String?      @db.Uuid
  contactId        String?      @db.Uuid
  originalFilename String
  storageKey       String
  mimeType         String
  fileSize         Int
  uploadedById     String?      @db.Uuid
  createdAt        DateTime     @default(now())
  updatedAt        DateTime     @updatedAt
  deletedAt        DateTime?
  organisation     Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  company          Company?     @relation(fields: [companyId], references: [id], onDelete: Cascade)
  contact          Contact?     @relation(fields: [contactId], references: [id], onDelete: Cascade)
  uploadedBy       User?        @relation("CustomerFileUploader", fields: [uploadedById], references: [id], onDelete: SetNull)

  @@unique([organisationId, storageKey])
  @@index([organisationId, companyId])
  @@index([organisationId, contactId])
  @@index([organisationId, uploadedById])
  @@index([organisationId, mimeType])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model CustomerActivity {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  companyId      String?              @db.Uuid
  contactId      String?              @db.Uuid
  leadId         String?              @db.Uuid
  opportunityId  String?              @db.Uuid
  actorUserId    String?              @db.Uuid
  type           CustomerActivityType
  title          String
  description    String?
  metadata       Json?
  createdAt      DateTime             @default(now())
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  company        Company?             @relation(fields: [companyId], references: [id], onDelete: Cascade)
  contact        Contact?             @relation(fields: [contactId], references: [id], onDelete: Cascade)
  lead           Lead?                @relation(fields: [leadId], references: [id], onDelete: Cascade)
  opportunity    Opportunity?         @relation(fields: [opportunityId], references: [id], onDelete: Cascade)
  actorUser      User?                @relation("CustomerActivityActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, companyId, createdAt])
  @@index([organisationId, contactId, createdAt])
  @@index([organisationId, leadId, createdAt])
  @@index([organisationId, opportunityId, createdAt])
  @@index([organisationId, type])
  @@index([organisationId, createdAt])
}

model CustomerTag {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  name           String
  color          String       @default("#111827")
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  companies      CompanyTag[]

  @@unique([organisationId, name])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model CompanyTag {
  id        String      @id @default(uuid()) @db.Uuid
  companyId String      @db.Uuid
  tagId     String      @db.Uuid
  createdAt DateTime    @default(now())
  company   Company     @relation(fields: [companyId], references: [id], onDelete: Cascade)
  tag       CustomerTag @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@unique([companyId, tagId])
  @@index([tagId])
}

model Project {
  id                String                   @id @default(uuid()) @db.Uuid
  organisationId    String                   @db.Uuid
  companyId         String?                  @db.Uuid
  ownerId           String                   @db.Uuid
  name              String
  description       String?
  status            ProjectStatus            @default(PLANNED)
  priority          ProjectPriority          @default(MEDIUM)
  progress          Int                      @default(0)
  budget            Decimal?                 @db.Decimal(14, 2)
  estimatedHours    Decimal?                 @db.Decimal(10, 2)
  actualHours       Decimal?                 @db.Decimal(10, 2)
  startDate         DateTime?
  dueDate           DateTime?
  completedAt       DateTime?
  archivedAt        DateTime?
  createdAt         DateTime                 @default(now())
  updatedAt         DateTime                 @updatedAt
  deletedAt         DateTime?
  organisation      Organisation             @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  company           Company?                 @relation(fields: [companyId], references: [id], onDelete: SetNull)
  owner             User                     @relation("ProjectOwner", fields: [ownerId], references: [id], onDelete: Restrict)
  tasks             Task[]
  milestones        ProjectMilestone[]
  comments          ProjectComment[]
  files             ProjectFile[]
  boards            ProjectBoard[]
  activities        ProjectActivity[]
  conversations     Conversation[]
  meetings          Meeting[]
  meetingNotes      MeetingNote[]
  notificationPrefs NotificationPreference[]
  invoices          Invoice[]
  quotes            Quote[]
  expenses          Expense[]
  purchaseOrders    PurchaseOrder[]
  budgets           Budget[]
  cashFlowEntries   CashFlowEntry[]

  @@index([organisationId, name])
  @@index([organisationId, companyId])
  @@index([organisationId, ownerId])
  @@index([organisationId, status])
  @@index([organisationId, priority])
  @@index([organisationId, dueDate])
  @@index([organisationId, archivedAt])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model Task {
  id             String            @id @default(uuid()) @db.Uuid
  organisationId String            @db.Uuid
  projectId      String            @db.Uuid
  parentTaskId   String?           @db.Uuid
  title          String
  description    String?
  status         TaskStatus        @default(TODO)
  priority       TaskPriority      @default(MEDIUM)
  assigneeId     String?           @db.Uuid
  reporterId     String?           @db.Uuid
  estimatedHours Decimal?          @db.Decimal(10, 2)
  actualHours    Decimal?          @db.Decimal(10, 2)
  dueDate        DateTime?
  position       Int               @default(0)
  completedAt    DateTime?
  createdAt      DateTime          @default(now())
  updatedAt      DateTime          @updatedAt
  deletedAt      DateTime?
  organisation   Organisation      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project        Project           @relation(fields: [projectId], references: [id], onDelete: Cascade)
  parentTask     Task?             @relation("TaskSubtasks", fields: [parentTaskId], references: [id], onDelete: SetNull)
  subtasks       Task[]            @relation("TaskSubtasks")
  assignee       User?             @relation("TaskAssignee", fields: [assigneeId], references: [id], onDelete: SetNull)
  reporter       User?             @relation("TaskReporter", fields: [reporterId], references: [id], onDelete: SetNull)
  comments       ProjectComment[]
  files          ProjectFile[]
  labels         TaskLabel[]
  timeEntries    TimeEntry[]
  activities     ProjectActivity[]
  meetings       Meeting[]
  meetingNotes   MeetingNote[]

  @@index([organisationId, projectId])
  @@index([organisationId, parentTaskId])
  @@index([organisationId, assigneeId])
  @@index([organisationId, reporterId])
  @@index([organisationId, status])
  @@index([organisationId, priority])
  @@index([organisationId, dueDate])
  @@index([organisationId, position])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model ProjectMilestone {
  id          String            @id @default(uuid()) @db.Uuid
  projectId   String            @db.Uuid
  title       String
  description String?
  dueDate     DateTime?
  status      MilestoneStatus   @default(PLANNED)
  progress    Int               @default(0)
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt
  deletedAt   DateTime?
  project     Project           @relation(fields: [projectId], references: [id], onDelete: Cascade)
  activities  ProjectActivity[]

  @@index([projectId, status])
  @@index([projectId, dueDate])
  @@index([projectId, deletedAt])
}

model ProjectLabel {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  name           String
  colour         String       @default("#111827")
  icon           String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  tasks          TaskLabel[]

  @@unique([organisationId, name])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model TaskLabel {
  id        String       @id @default(uuid()) @db.Uuid
  taskId    String       @db.Uuid
  labelId   String       @db.Uuid
  createdAt DateTime     @default(now())
  task      Task         @relation(fields: [taskId], references: [id], onDelete: Cascade)
  label     ProjectLabel @relation(fields: [labelId], references: [id], onDelete: Cascade)

  @@unique([taskId, labelId])
  @@index([labelId])
}

model ProjectComment {
  id              String           @id @default(uuid()) @db.Uuid
  organisationId  String           @db.Uuid
  projectId       String           @db.Uuid
  taskId          String?          @db.Uuid
  parentCommentId String?          @db.Uuid
  authorId        String?          @db.Uuid
  content         String
  mentions        Json?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  deletedAt       DateTime?
  organisation    Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project         Project          @relation(fields: [projectId], references: [id], onDelete: Cascade)
  task            Task?            @relation(fields: [taskId], references: [id], onDelete: Cascade)
  parentComment   ProjectComment?  @relation("ProjectCommentReplies", fields: [parentCommentId], references: [id], onDelete: SetNull)
  replies         ProjectComment[] @relation("ProjectCommentReplies")
  author          User?            @relation("ProjectCommentAuthor", fields: [authorId], references: [id], onDelete: SetNull)
  files           ProjectFile[]

  @@index([organisationId, projectId, createdAt])
  @@index([organisationId, taskId, createdAt])
  @@index([organisationId, parentCommentId])
  @@index([organisationId, authorId])
  @@index([organisationId, deletedAt])
}

model ProjectFile {
  id               String          @id @default(uuid()) @db.Uuid
  organisationId   String          @db.Uuid
  projectId        String          @db.Uuid
  taskId           String?         @db.Uuid
  commentId        String?         @db.Uuid
  storageKey       String
  originalFilename String
  mimeType         String
  fileSize         Int
  uploadedById     String?         @db.Uuid
  createdAt        DateTime        @default(now())
  updatedAt        DateTime        @updatedAt
  deletedAt        DateTime?
  organisation     Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project          Project         @relation(fields: [projectId], references: [id], onDelete: Cascade)
  task             Task?           @relation(fields: [taskId], references: [id], onDelete: Cascade)
  comment          ProjectComment? @relation(fields: [commentId], references: [id], onDelete: SetNull)
  uploadedBy       User?           @relation("ProjectFileUploader", fields: [uploadedById], references: [id], onDelete: SetNull)

  @@unique([organisationId, storageKey])
  @@index([organisationId, projectId])
  @@index([organisationId, taskId])
  @@index([organisationId, commentId])
  @@index([organisationId, uploadedById])
  @@index([organisationId, mimeType])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model TimeEntry {
  id              String       @id @default(uuid()) @db.Uuid
  organisationId  String       @db.Uuid
  taskId          String       @db.Uuid
  userId          String       @db.Uuid
  startTime       DateTime
  endTime         DateTime?
  durationMinutes Int?
  manualEntry     Boolean      @default(false)
  description     String?
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt
  deletedAt       DateTime?
  organisation    Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  task            Task         @relation(fields: [taskId], references: [id], onDelete: Cascade)
  user            User         @relation("TimeEntryUser", fields: [userId], references: [id], onDelete: Cascade)

  @@index([organisationId, taskId])
  @@index([organisationId, userId])
  @@index([organisationId, startTime])
  @@index([organisationId, deletedAt])
}

model ProjectBoard {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  projectId      String               @db.Uuid
  name           String
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project        Project              @relation(fields: [projectId], references: [id], onDelete: Cascade)
  columns        ProjectBoardColumn[]

  @@unique([projectId, name])
  @@index([organisationId, projectId])
  @@index([organisationId, deletedAt])
}

model ProjectBoardColumn {
  id        String       @id @default(uuid()) @db.Uuid
  boardId   String       @db.Uuid
  name      String
  status    TaskStatus?
  position  Int          @default(0)
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt
  board     ProjectBoard @relation(fields: [boardId], references: [id], onDelete: Cascade)

  @@index([boardId, position])
}

model ProjectActivity {
  id             String              @id @default(uuid()) @db.Uuid
  organisationId String              @db.Uuid
  projectId      String              @db.Uuid
  taskId         String?             @db.Uuid
  milestoneId    String?             @db.Uuid
  actorUserId    String?             @db.Uuid
  type           ProjectActivityType
  title          String
  description    String?
  metadata       Json?
  createdAt      DateTime            @default(now())
  organisation   Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project        Project             @relation(fields: [projectId], references: [id], onDelete: Cascade)
  task           Task?               @relation(fields: [taskId], references: [id], onDelete: Cascade)
  milestone      ProjectMilestone?   @relation(fields: [milestoneId], references: [id], onDelete: Cascade)
  actorUser      User?               @relation("ProjectActivityActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, projectId, createdAt])
  @@index([organisationId, taskId, createdAt])
  @@index([organisationId, milestoneId, createdAt])
  @@index([organisationId, actorUserId])
  @@index([organisationId, type])
  @@index([organisationId, createdAt])
}

model Folder {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  parentFolderId String?              @db.Uuid
  ownerId        String               @db.Uuid
  name           String
  description    String?
  path           String
  depth          Int                  @default(0)
  colour         String?
  icon           String?
  visibility     DocumentVisibility   @default(ORGANISATION)
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  parentFolder   Folder?              @relation("FolderTree", fields: [parentFolderId], references: [id], onDelete: SetNull)
  childFolders   Folder[]             @relation("FolderTree")
  owner          User                 @relation("FolderOwner", fields: [ownerId], references: [id], onDelete: Restrict)
  documents      Document[]
  permissions    DocumentPermission[]
  activities     DocumentActivity[]

  @@unique([organisationId, parentFolderId, name])
  @@index([organisationId, parentFolderId])
  @@index([organisationId, ownerId])
  @@index([organisationId, path])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model Document {
  id               String                  @id @default(uuid()) @db.Uuid
  organisationId   String                  @db.Uuid
  folderId         String?                 @db.Uuid
  ownerId          String                  @db.Uuid
  title            String
  description      String?
  fileName         String
  originalFileName String
  mimeType         String
  fileExtension    String
  fileSize         Int
  storageKey       String
  storageBucket    String
  checksum         String
  documentType     DocumentType            @default(OTHER)
  status           DocumentStatus          @default(ACTIVE)
  visibility       DocumentVisibility      @default(ORGANISATION)
  isPinned         Boolean                 @default(false)
  isLocked         Boolean                 @default(false)
  currentVersionId String?                 @unique @db.Uuid
  createdAt        DateTime                @default(now())
  updatedAt        DateTime                @updatedAt
  deletedAt        DateTime?
  organisation     Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  folder           Folder?                 @relation(fields: [folderId], references: [id], onDelete: SetNull)
  owner            User                    @relation("DocumentOwner", fields: [ownerId], references: [id], onDelete: Restrict)
  currentVersion   DocumentVersion?        @relation("DocumentCurrentVersion", fields: [currentVersionId], references: [id], onDelete: SetNull)
  versions         DocumentVersion[]       @relation("DocumentVersions")
  permissions      DocumentPermission[]
  activities       DocumentActivity[]
  tags             DocumentTagAssignment[]
  links            DocumentLink[]
  shares           DocumentShare[]
  meetings         Meeting[]
  meetingNotes     MeetingNote[]

  @@unique([organisationId, storageKey])
  @@index([organisationId, folderId])
  @@index([organisationId, ownerId])
  @@index([organisationId, title])
  @@index([organisationId, documentType])
  @@index([organisationId, status])
  @@index([organisationId, visibility])
  @@index([organisationId, mimeType])
  @@index([organisationId, checksum])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
  @@index([organisationId, updatedAt])
}

model DocumentVersion {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  documentId     String       @db.Uuid
  versionNumber  Int
  fileName       String
  storageKey     String
  mimeType       String
  fileSize       Int
  checksum       String
  uploadedById   String?      @db.Uuid
  changeNote     String?
  createdAt      DateTime     @default(now())
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  document       Document     @relation("DocumentVersions", fields: [documentId], references: [id], onDelete: Cascade)
  currentFor     Document?    @relation("DocumentCurrentVersion")
  uploadedBy     User?        @relation("DocumentVersionUploader", fields: [uploadedById], references: [id], onDelete: SetNull)

  @@unique([documentId, versionNumber])
  @@unique([organisationId, storageKey])
  @@index([organisationId, documentId])
  @@index([organisationId, uploadedById])
  @@index([organisationId, createdAt])
}

model DocumentPermission {
  id              String                  @id @default(uuid()) @db.Uuid
  organisationId  String                  @db.Uuid
  documentId      String?                 @db.Uuid
  folderId        String?                 @db.Uuid
  userId          String?                 @db.Uuid
  teamId          String?                 @db.Uuid
  roleId          String?                 @db.Uuid
  permissionLevel DocumentPermissionLevel
  createdAt       DateTime                @default(now())
  updatedAt       DateTime                @updatedAt
  organisation    Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  document        Document?               @relation(fields: [documentId], references: [id], onDelete: Cascade)
  folder          Folder?                 @relation(fields: [folderId], references: [id], onDelete: Cascade)
  user            User?                   @relation("DocumentPermissionUser", fields: [userId], references: [id], onDelete: Cascade)
  role            Role?                   @relation(fields: [roleId], references: [id], onDelete: Cascade)

  @@index([organisationId, documentId])
  @@index([organisationId, folderId])
  @@index([organisationId, userId])
  @@index([organisationId, teamId])
  @@index([organisationId, roleId])
  @@index([organisationId, permissionLevel])
}

model DocumentActivity {
  id             String                 @id @default(uuid()) @db.Uuid
  organisationId String                 @db.Uuid
  documentId     String?                @db.Uuid
  folderId       String?                @db.Uuid
  actorId        String?                @db.Uuid
  action         DocumentActivityAction
  metadata       Json?
  createdAt      DateTime               @default(now())
  organisation   Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  document       Document?              @relation(fields: [documentId], references: [id], onDelete: Cascade)
  folder         Folder?                @relation(fields: [folderId], references: [id], onDelete: Cascade)
  actor          User?                  @relation("DocumentActivityActor", fields: [actorId], references: [id], onDelete: SetNull)

  @@index([organisationId, documentId, createdAt])
  @@index([organisationId, folderId, createdAt])
  @@index([organisationId, actorId])
  @@index([organisationId, action])
  @@index([organisationId, createdAt])
}

model DocumentTag {
  id             String                  @id @default(uuid()) @db.Uuid
  organisationId String                  @db.Uuid
  name           String
  colour         String                  @default("#111827")
  description    String?
  createdAt      DateTime                @default(now())
  updatedAt      DateTime                @updatedAt
  deletedAt      DateTime?
  organisation   Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  documents      DocumentTagAssignment[]

  @@unique([organisationId, name])
  @@index([organisationId, deletedAt])
  @@index([organisationId, createdAt])
}

model DocumentTagAssignment {
  id             String      @id @default(uuid()) @db.Uuid
  organisationId String      @db.Uuid
  documentId     String      @db.Uuid
  tagId          String      @db.Uuid
  createdAt      DateTime    @default(now())
  document       Document    @relation(fields: [documentId], references: [id], onDelete: Cascade)
  tag            DocumentTag @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@unique([documentId, tagId])
  @@index([organisationId, documentId])
  @@index([organisationId, tagId])
}

model DocumentLink {
  id             String                 @id @default(uuid()) @db.Uuid
  organisationId String                 @db.Uuid
  documentId     String                 @db.Uuid
  entityType     DocumentLinkEntityType
  entityId       String                 @db.Uuid
  createdAt      DateTime               @default(now())
  organisation   Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  document       Document               @relation(fields: [documentId], references: [id], onDelete: Cascade)

  @@unique([documentId, entityType, entityId])
  @@index([organisationId, documentId])
  @@index([organisationId, entityType, entityId])
  @@index([organisationId, createdAt])
}

model DocumentShare {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  documentId     String       @db.Uuid
  tokenHash      String       @unique
  expiresAt      DateTime?
  createdById    String       @db.Uuid
  createdAt      DateTime     @default(now())
  revokedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  document       Document     @relation(fields: [documentId], references: [id], onDelete: Cascade)
  createdBy      User         @relation("DocumentShareCreator", fields: [createdById], references: [id], onDelete: Restrict)

  @@index([organisationId, documentId])
  @@index([organisationId, expiresAt])
  @@index([organisationId, revokedAt])
}

model Conversation {
  id             String                 @id @default(uuid()) @db.Uuid
  organisationId String                 @db.Uuid
  name           String?
  description    String?
  type           ConversationType
  visibility     ConversationVisibility @default(PRIVATE)
  department     String?
  projectId      String?                @db.Uuid
  isArchived     Boolean                @default(false)
  isPinned       Boolean                @default(false)
  createdById    String?                @db.Uuid
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  deletedAt      DateTime?
  organisation   Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project        Project?               @relation(fields: [projectId], references: [id], onDelete: SetNull)
  createdBy      User?                  @relation("ConversationCreator", fields: [createdById], references: [id], onDelete: SetNull)
  members        ConversationMember[]
  messages       Message[]
  threads        Thread[]
  announcements  Announcement[]
  activities     MessageActivity[]

  @@index([organisationId, type])
  @@index([organisationId, visibility])
  @@index([organisationId, projectId])
  @@index([organisationId, isArchived])
  @@index([organisationId, deletedAt])
  @@index([organisationId, updatedAt])
}

model ConversationMember {
  id             String                 @id @default(uuid()) @db.Uuid
  organisationId String                 @db.Uuid
  conversationId String                 @db.Uuid
  userId         String                 @db.Uuid
  role           ConversationMemberRole @default(MEMBER)
  mutedUntil     DateTime?
  lastReadAt     DateTime?
  joinedAt       DateTime               @default(now())
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  deletedAt      DateTime?
  organisation   Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  conversation   Conversation           @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  user           User                   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([conversationId, userId])
  @@index([organisationId, userId])
  @@index([organisationId, conversationId])
  @@index([deletedAt])
}

model Message {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  conversationId String               @db.Uuid
  threadId       String?              @db.Uuid
  authorId       String               @db.Uuid
  parentId       String?              @db.Uuid
  content        String
  markdown       Boolean              @default(true)
  mentions       String[]             @default([])
  status         MessageStatus        @default(ACTIVE)
  editedAt       DateTime?
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  conversation   Conversation         @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  thread         Thread?              @relation(fields: [threadId], references: [id], onDelete: SetNull)
  author         User                 @relation("MessageAuthor", fields: [authorId], references: [id], onDelete: Restrict)
  parent         Message?             @relation("MessageReplies", fields: [parentId], references: [id], onDelete: SetNull)
  replies        Message[]            @relation("MessageReplies")
  attachments    MessageAttachment[]
  reactions      MessageReaction[]
  readReceipts   MessageReadReceipt[]
  activities     MessageActivity[]
  startedThread  Thread?              @relation("ThreadRootMessage")

  @@index([organisationId, conversationId, createdAt])
  @@index([organisationId, threadId])
  @@index([organisationId, authorId])
  @@index([organisationId, parentId])
  @@index([organisationId, status])
  @@index([organisationId, deletedAt])
}

model MessageAttachment {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  messageId      String       @db.Uuid
  uploadedById   String       @db.Uuid
  fileName       String
  storageKey     String
  mimeType       String
  fileSize       Int
  checksum       String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  message        Message      @relation(fields: [messageId], references: [id], onDelete: Cascade)
  uploadedBy     User         @relation("MessageAttachmentUploader", fields: [uploadedById], references: [id], onDelete: Restrict)

  @@index([organisationId, messageId])
  @@index([organisationId, uploadedById])
  @@index([organisationId, deletedAt])
}

model MessageReaction {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  messageId      String       @db.Uuid
  userId         String       @db.Uuid
  emoji          String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  message        Message      @relation(fields: [messageId], references: [id], onDelete: Cascade)
  user           User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([messageId, userId, emoji])
  @@index([organisationId, messageId])
  @@index([organisationId, userId])
  @@index([deletedAt])
}

model MessageReadReceipt {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  messageId      String       @db.Uuid
  userId         String       @db.Uuid
  readAt         DateTime     @default(now())
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  message        Message      @relation(fields: [messageId], references: [id], onDelete: Cascade)
  user           User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([messageId, userId])
  @@index([organisationId, userId])
  @@index([organisationId, readAt])
}

model Thread {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  conversationId String       @db.Uuid
  rootMessageId  String       @unique @db.Uuid
  title          String?
  createdById    String       @db.Uuid
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  conversation   Conversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)
  rootMessage    Message      @relation("ThreadRootMessage", fields: [rootMessageId], references: [id], onDelete: Cascade)
  createdBy      User         @relation("ThreadCreator", fields: [createdById], references: [id], onDelete: Restrict)
  messages       Message[]

  @@index([organisationId, conversationId])
  @@index([organisationId, createdById])
  @@index([organisationId, deletedAt])
}

model Announcement {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  conversationId String?              @db.Uuid
  authorId       String               @db.Uuid
  title          String
  body           String
  priority       AnnouncementPriority @default(NORMAL)
  isPinned       Boolean              @default(false)
  expiresAt      DateTime?
  publishedAt    DateTime?
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  conversation   Conversation?        @relation(fields: [conversationId], references: [id], onDelete: SetNull)
  author         User                 @relation("AnnouncementAuthor", fields: [authorId], references: [id], onDelete: Restrict)

  @@index([organisationId, priority])
  @@index([organisationId, isPinned])
  @@index([organisationId, publishedAt])
  @@index([organisationId, expiresAt])
  @@index([organisationId, deletedAt])
}

model Meeting {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  organizerId    String               @db.Uuid
  title          String
  description    String?
  agenda         String?
  location       String?
  videoUrl       String?
  status         MeetingStatus        @default(SCHEDULED)
  startsAt       DateTime
  endsAt         DateTime
  projectId      String?              @db.Uuid
  companyId      String?              @db.Uuid
  documentId     String?              @db.Uuid
  taskId         String?              @db.Uuid
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  organizer      User                 @relation("MeetingOrganizer", fields: [organizerId], references: [id], onDelete: Restrict)
  project        Project?             @relation(fields: [projectId], references: [id], onDelete: SetNull)
  company        Company?             @relation(fields: [companyId], references: [id], onDelete: SetNull)
  document       Document?            @relation(fields: [documentId], references: [id], onDelete: SetNull)
  task           Task?                @relation(fields: [taskId], references: [id], onDelete: SetNull)
  participants   MeetingParticipant[]
  notes          MeetingNote[]
  recordings     MeetingRecording[]

  @@index([organisationId, organizerId])
  @@index([organisationId, startsAt])
  @@index([organisationId, status])
  @@index([organisationId, projectId])
  @@index([organisationId, companyId])
  @@index([organisationId, deletedAt])
}

model MeetingParticipant {
  id             String                   @id @default(uuid()) @db.Uuid
  organisationId String                   @db.Uuid
  meetingId      String                   @db.Uuid
  userId         String?                  @db.Uuid
  email          String?
  name           String?
  status         MeetingParticipantStatus @default(INVITED)
  role           String?
  createdAt      DateTime                 @default(now())
  updatedAt      DateTime                 @updatedAt
  deletedAt      DateTime?
  organisation   Organisation             @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  meeting        Meeting                  @relation(fields: [meetingId], references: [id], onDelete: Cascade)
  user           User?                    @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([organisationId, meetingId])
  @@index([organisationId, userId])
  @@index([organisationId, status])
  @@index([deletedAt])
}

model MeetingNote {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  meetingId      String       @db.Uuid
  authorId       String       @db.Uuid
  title          String
  content        String
  actionItems    Json?
  projectId      String?      @db.Uuid
  companyId      String?      @db.Uuid
  documentId     String?      @db.Uuid
  taskId         String?      @db.Uuid
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  meeting        Meeting      @relation(fields: [meetingId], references: [id], onDelete: Cascade)
  author         User         @relation("MeetingNoteAuthor", fields: [authorId], references: [id], onDelete: Restrict)
  project        Project?     @relation(fields: [projectId], references: [id], onDelete: SetNull)
  company        Company?     @relation(fields: [companyId], references: [id], onDelete: SetNull)
  document       Document?    @relation(fields: [documentId], references: [id], onDelete: SetNull)
  task           Task?        @relation(fields: [taskId], references: [id], onDelete: SetNull)

  @@index([organisationId, meetingId])
  @@index([organisationId, authorId])
  @@index([organisationId, projectId])
  @@index([organisationId, companyId])
  @@index([organisationId, deletedAt])
}

model MeetingRecording {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  meetingId      String       @db.Uuid
  title          String
  storageKey     String?
  durationSecs   Int?
  transcriptKey  String?
  status         String       @default("PLACEHOLDER")
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  meeting        Meeting      @relation(fields: [meetingId], references: [id], onDelete: Cascade)

  @@index([organisationId, meetingId])
  @@index([organisationId, status])
  @@index([organisationId, deletedAt])
}

model MessageActivity {
  id             String                @id @default(uuid()) @db.Uuid
  organisationId String                @db.Uuid
  conversationId String?               @db.Uuid
  messageId      String?               @db.Uuid
  actorId        String?               @db.Uuid
  action         MessageActivityAction
  metadata       Json?
  createdAt      DateTime              @default(now())
  updatedAt      DateTime              @updatedAt
  deletedAt      DateTime?
  organisation   Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  conversation   Conversation?         @relation(fields: [conversationId], references: [id], onDelete: SetNull)
  message        Message?              @relation(fields: [messageId], references: [id], onDelete: SetNull)
  actor          User?                 @relation("MessageActivityActor", fields: [actorId], references: [id], onDelete: SetNull)

  @@index([organisationId, conversationId])
  @@index([organisationId, messageId])
  @@index([organisationId, action])
  @@index([organisationId, createdAt])
}

model NotificationPreference {
  id               String         @id @default(uuid()) @db.Uuid
  organisationId   String         @db.Uuid
  userId           String         @db.Uuid
  channel          String         @default("in_app")
  mentions         Boolean        @default(true)
  directMessages   Boolean        @default(true)
  announcements    Boolean        @default(true)
  meetings         Boolean        @default(true)
  quietHoursStart  String?
  quietHoursEnd    String?
  presenceStatus   PresenceStatus @default(OFFLINE)
  presenceMessage  String?
  currentProjectId String?        @db.Uuid
  lastSeenAt       DateTime?
  createdAt        DateTime       @default(now())
  updatedAt        DateTime       @updatedAt
  deletedAt        DateTime?
  organisation     Organisation   @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  user             User           @relation(fields: [userId], references: [id], onDelete: Cascade)
  currentProject   Project?       @relation(fields: [currentProjectId], references: [id], onDelete: SetNull)

  @@unique([organisationId, userId, channel])
  @@index([organisationId, userId])
  @@index([organisationId, presenceStatus])
  @@index([organisationId, deletedAt])
}

model Workflow {
  id             String              @id @default(uuid()) @db.Uuid
  organisationId String              @db.Uuid
  name           String
  description    String?
  status         WorkflowStatus      @default(DRAFT)
  enabled        Boolean             @default(false)
  triggerType    WorkflowTriggerType @default(MANUAL)
  createdById    String              @db.Uuid
  currentVersion Int                 @default(1)
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  deletedAt      DateTime?
  organisation   Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  createdBy      User                @relation("WorkflowCreator", fields: [createdById], references: [id], onDelete: Restrict)
  versions       WorkflowVersion[]
  executions     WorkflowExecution[]
  triggers       WorkflowTrigger[]
  conditions     WorkflowCondition[]
  actions        WorkflowAction[]
  variables      WorkflowVariable[]
  schedules      WorkflowSchedule[]
  approvals      WorkflowApproval[]
  logs           WorkflowLog[]
  audits         WorkflowAudit[]
  errors         WorkflowError[]

  @@index([organisationId, status])
  @@index([organisationId, enabled])
  @@index([organisationId, triggerType])
  @@index([organisationId, createdById])
  @@index([organisationId, deletedAt])
  @@index([organisationId, updatedAt])
}

model WorkflowVersion {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  workflowId     String       @db.Uuid
  version        Int
  definition     Json
  notes          String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow     @relation(fields: [workflowId], references: [id], onDelete: Cascade)

  @@unique([workflowId, version])
  @@index([organisationId, workflowId])
  @@index([organisationId, version])
}

model WorkflowExecution {
  id             String                  @id @default(uuid()) @db.Uuid
  organisationId String                  @db.Uuid
  workflowId     String                  @db.Uuid
  triggeredById  String?                 @db.Uuid
  status         WorkflowExecutionStatus @default(QUEUED)
  triggerPayload Json?
  context        Json?
  startedAt      DateTime?
  finishedAt     DateTime?
  durationMs     Int?
  retryCount     Int                     @default(0)
  createdAt      DateTime                @default(now())
  updatedAt      DateTime                @updatedAt
  deletedAt      DateTime?
  organisation   Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow                @relation(fields: [workflowId], references: [id], onDelete: Cascade)
  triggeredBy    User?                   @relation("WorkflowExecutionActor", fields: [triggeredById], references: [id], onDelete: SetNull)
  logs           WorkflowLog[]
  approvals      WorkflowApproval[]
  errors         WorkflowError[]

  @@index([organisationId, workflowId])
  @@index([organisationId, status])
  @@index([organisationId, triggeredById])
  @@index([organisationId, createdAt])
  @@index([organisationId, deletedAt])
}

model WorkflowTrigger {
  id             String              @id @default(uuid()) @db.Uuid
  organisationId String              @db.Uuid
  workflowId     String              @db.Uuid
  type           WorkflowTriggerType
  config         Json?
  enabled        Boolean             @default(true)
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  deletedAt      DateTime?
  organisation   Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow            @relation(fields: [workflowId], references: [id], onDelete: Cascade)

  @@index([organisationId, workflowId])
  @@index([organisationId, type])
  @@index([organisationId, enabled])
  @@index([organisationId, deletedAt])
}

model WorkflowCondition {
  id             String                    @id @default(uuid()) @db.Uuid
  organisationId String                    @db.Uuid
  workflowId     String                    @db.Uuid
  operator       WorkflowConditionOperator
  field          String
  value          Json?
  position       Int                       @default(0)
  createdAt      DateTime                  @default(now())
  updatedAt      DateTime                  @updatedAt
  deletedAt      DateTime?
  organisation   Organisation              @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow                  @relation(fields: [workflowId], references: [id], onDelete: Cascade)

  @@index([organisationId, workflowId])
  @@index([organisationId, operator])
  @@index([organisationId, position])
  @@index([organisationId, deletedAt])
}

model WorkflowAction {
  id             String             @id @default(uuid()) @db.Uuid
  organisationId String             @db.Uuid
  workflowId     String             @db.Uuid
  type           WorkflowActionType
  name           String
  config         Json?
  position       Int                @default(0)
  retryPolicy    Json?
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt
  deletedAt      DateTime?
  organisation   Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow           @relation(fields: [workflowId], references: [id], onDelete: Cascade)

  @@index([organisationId, workflowId])
  @@index([organisationId, type])
  @@index([organisationId, position])
  @@index([organisationId, deletedAt])
}

model WorkflowVariable {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  workflowId     String?      @db.Uuid
  key            String
  value          Json?
  isSecret       Boolean      @default(false)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow?    @relation(fields: [workflowId], references: [id], onDelete: Cascade)

  @@unique([organisationId, workflowId, key])
  @@index([organisationId, key])
  @@index([organisationId, deletedAt])
}

model WorkflowTemplate {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String?       @db.Uuid
  name           String
  description    String?
  category       String
  definition     Json
  isSystem       Boolean       @default(false)
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  deletedAt      DateTime?
  organisation   Organisation? @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, category])
  @@index([category])
  @@index([isSystem])
  @@index([deletedAt])
}

model WorkflowSchedule {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  workflowId     String               @db.Uuid
  type           WorkflowScheduleType
  cronExpression String?
  timezone       String               @default("UTC")
  nextRunAt      DateTime?
  lastRunAt      DateTime?
  enabled        Boolean              @default(true)
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow             @relation(fields: [workflowId], references: [id], onDelete: Cascade)

  @@index([organisationId, workflowId])
  @@index([organisationId, nextRunAt])
  @@index([organisationId, enabled])
  @@index([organisationId, deletedAt])
}

model WorkflowApproval {
  id             String                 @id @default(uuid()) @db.Uuid
  organisationId String                 @db.Uuid
  workflowId     String                 @db.Uuid
  executionId    String?                @db.Uuid
  requestedById  String?                @db.Uuid
  reviewerId     String?                @db.Uuid
  status         WorkflowApprovalStatus @default(PENDING)
  title          String
  details        Json?
  decidedAt      DateTime?
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  deletedAt      DateTime?
  organisation   Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow               @relation(fields: [workflowId], references: [id], onDelete: Cascade)
  execution      WorkflowExecution?     @relation(fields: [executionId], references: [id], onDelete: SetNull)
  requestedBy    User?                  @relation("WorkflowApprovalRequester", fields: [requestedById], references: [id], onDelete: SetNull)
  reviewer       User?                  @relation("WorkflowApprovalReviewer", fields: [reviewerId], references: [id], onDelete: SetNull)

  @@index([organisationId, workflowId])
  @@index([organisationId, executionId])
  @@index([organisationId, status])
  @@index([organisationId, reviewerId])
  @@index([organisationId, deletedAt])
}

model WorkflowLog {
  id             String              @id @default(uuid()) @db.Uuid
  organisationId String              @db.Uuid
  workflowId     String?             @db.Uuid
  executionId    String?             @db.Uuid
  severity       WorkflowLogSeverity @default(INFO)
  step           String?
  message        String
  metadata       Json?
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  deletedAt      DateTime?
  organisation   Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow?           @relation(fields: [workflowId], references: [id], onDelete: SetNull)
  execution      WorkflowExecution?  @relation(fields: [executionId], references: [id], onDelete: SetNull)

  @@index([organisationId, workflowId])
  @@index([organisationId, executionId])
  @@index([organisationId, severity])
  @@index([organisationId, createdAt])
}

model WorkflowAudit {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  workflowId     String?      @db.Uuid
  actorUserId    String?      @db.Uuid
  action         String
  metadata       Json?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow?    @relation(fields: [workflowId], references: [id], onDelete: SetNull)
  actorUser      User?        @relation("WorkflowAuditActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, workflowId])
  @@index([organisationId, actorUserId])
  @@index([organisationId, action])
  @@index([organisationId, createdAt])
}

model WorkflowError {
  id             String             @id @default(uuid()) @db.Uuid
  organisationId String             @db.Uuid
  workflowId     String?            @db.Uuid
  executionId    String?            @db.Uuid
  code           String?
  message        String
  stack          String?
  retryable      Boolean            @default(false)
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt
  deletedAt      DateTime?
  organisation   Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  workflow       Workflow?          @relation(fields: [workflowId], references: [id], onDelete: SetNull)
  execution      WorkflowExecution? @relation(fields: [executionId], references: [id], onDelete: SetNull)

  @@index([organisationId, workflowId])
  @@index([organisationId, executionId])
  @@index([organisationId, retryable])
  @@index([organisationId, createdAt])
}

model Invoice {
  id             String                @id @default(uuid()) @db.Uuid
  organisationId String                @db.Uuid
  customerId     String?               @db.Uuid
  projectId      String?               @db.Uuid
  invoiceNumber  String
  status         FinanceDocumentStatus @default(DRAFT)
  currency       String                @default("USD")
  subtotal       Decimal               @default(0) @db.Decimal(14, 2)
  tax            Decimal               @default(0) @db.Decimal(14, 2)
  discount       Decimal               @default(0) @db.Decimal(14, 2)
  total          Decimal               @default(0) @db.Decimal(14, 2)
  amountPaid     Decimal               @default(0) @db.Decimal(14, 2)
  balance        Decimal               @default(0) @db.Decimal(14, 2)
  issueDate      DateTime
  dueDate        DateTime?
  paidDate       DateTime?
  notes          String?
  metadata       Json?
  createdAt      DateTime              @default(now())
  updatedAt      DateTime              @updatedAt
  deletedAt      DateTime?
  organisation   Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  customer       Company?              @relation(fields: [customerId], references: [id], onDelete: SetNull)
  project        Project?              @relation(fields: [projectId], references: [id], onDelete: SetNull)
  items          InvoiceItem[]
  payments       Payment[]
  recurringRules RecurringInvoice[]

  @@unique([organisationId, invoiceNumber])
  @@index([organisationId, customerId])
  @@index([organisationId, projectId])
  @@index([organisationId, status])
  @@index([organisationId, dueDate])
  @@index([organisationId, issueDate])
  @@index([organisationId, deletedAt])
}

model InvoiceItem {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  invoiceId      String       @db.Uuid
  description    String
  quantity       Decimal      @default(1) @db.Decimal(12, 2)
  unitPrice      Decimal      @default(0) @db.Decimal(14, 2)
  taxRate        Decimal      @default(0) @db.Decimal(6, 3)
  discount       Decimal      @default(0) @db.Decimal(14, 2)
  total          Decimal      @default(0) @db.Decimal(14, 2)
  position       Int          @default(0)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  invoice        Invoice      @relation(fields: [invoiceId], references: [id], onDelete: Cascade)

  @@index([organisationId, invoiceId])
  @@index([organisationId, deletedAt])
}

model Quote {
  id                 String       @id @default(uuid()) @db.Uuid
  organisationId     String       @db.Uuid
  customerId         String?      @db.Uuid
  projectId          String?      @db.Uuid
  quoteNumber        String
  version            Int          @default(1)
  status             QuoteStatus  @default(DRAFT)
  currency           String       @default("USD")
  subtotal           Decimal      @default(0) @db.Decimal(14, 2)
  tax                Decimal      @default(0) @db.Decimal(14, 2)
  discount           Decimal      @default(0) @db.Decimal(14, 2)
  total              Decimal      @default(0) @db.Decimal(14, 2)
  issueDate          DateTime     @default(now())
  expiryDate         DateTime?
  approvedAt         DateTime?
  convertedAt        DateTime?
  convertedInvoiceId String?      @db.Uuid
  notes              String?
  metadata           Json?
  createdAt          DateTime     @default(now())
  updatedAt          DateTime     @updatedAt
  deletedAt          DateTime?
  organisation       Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  customer           Company?     @relation(fields: [customerId], references: [id], onDelete: SetNull)
  project            Project?     @relation(fields: [projectId], references: [id], onDelete: SetNull)
  items              QuoteItem[]

  @@unique([organisationId, quoteNumber, version])
  @@index([organisationId, customerId])
  @@index([organisationId, projectId])
  @@index([organisationId, status])
  @@index([organisationId, expiryDate])
  @@index([organisationId, deletedAt])
}

model QuoteItem {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  quoteId        String       @db.Uuid
  description    String
  quantity       Decimal      @default(1) @db.Decimal(12, 2)
  unitPrice      Decimal      @default(0) @db.Decimal(14, 2)
  taxRate        Decimal      @default(0) @db.Decimal(6, 3)
  discount       Decimal      @default(0) @db.Decimal(14, 2)
  total          Decimal      @default(0) @db.Decimal(14, 2)
  position       Int          @default(0)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  quote          Quote        @relation(fields: [quoteId], references: [id], onDelete: Cascade)

  @@index([organisationId, quoteId])
  @@index([organisationId, deletedAt])
}

model ExpenseCategory {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  name           String
  colour         String       @default("#111827")
  description    String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  expenses       Expense[]

  @@unique([organisationId, name])
  @@index([organisationId, deletedAt])
}

model Expense {
  id             String           @id @default(uuid()) @db.Uuid
  organisationId String           @db.Uuid
  categoryId     String?          @db.Uuid
  customerId     String?          @db.Uuid
  projectId      String?          @db.Uuid
  title          String
  vendor         String?
  status         ExpenseStatus    @default(DRAFT)
  currency       String           @default("USD")
  amount         Decimal          @default(0) @db.Decimal(14, 2)
  tax            Decimal          @default(0) @db.Decimal(14, 2)
  total          Decimal          @default(0) @db.Decimal(14, 2)
  expenseDate    DateTime         @default(now())
  receiptUrl     String?
  department     String?
  tags           Json?
  approvedAt     DateTime?
  paidAt         DateTime?
  notes          String?
  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt
  deletedAt      DateTime?
  organisation   Organisation     @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  category       ExpenseCategory? @relation(fields: [categoryId], references: [id], onDelete: SetNull)
  customer       Company?         @relation(fields: [customerId], references: [id], onDelete: SetNull)
  project        Project?         @relation(fields: [projectId], references: [id], onDelete: SetNull)

  @@index([organisationId, categoryId])
  @@index([organisationId, customerId])
  @@index([organisationId, projectId])
  @@index([organisationId, status])
  @@index([organisationId, expenseDate])
  @@index([organisationId, deletedAt])
}

model Payment {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String        @db.Uuid
  invoiceId      String?       @db.Uuid
  customerId     String?       @db.Uuid
  method         PaymentMethod @default(MANUAL)
  status         PaymentStatus @default(PENDING)
  currency       String        @default("USD")
  amount         Decimal       @default(0) @db.Decimal(14, 2)
  paymentDate    DateTime      @default(now())
  reference      String?
  processor      String?
  notes          String?
  metadata       Json?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  deletedAt      DateTime?
  organisation   Organisation  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  invoice        Invoice?      @relation(fields: [invoiceId], references: [id], onDelete: SetNull)
  customer       Company?      @relation(fields: [customerId], references: [id], onDelete: SetNull)

  @@index([organisationId, invoiceId])
  @@index([organisationId, customerId])
  @@index([organisationId, status])
  @@index([organisationId, method])
  @@index([organisationId, paymentDate])
  @@index([organisationId, deletedAt])
}

model PurchaseOrder {
  id                   String              @id @default(uuid()) @db.Uuid
  organisationId       String              @db.Uuid
  supplierId           String?             @db.Uuid
  projectId            String?             @db.Uuid
  orderNumber          String
  status               PurchaseOrderStatus @default(DRAFT)
  currency             String              @default("USD")
  subtotal             Decimal             @default(0) @db.Decimal(14, 2)
  tax                  Decimal             @default(0) @db.Decimal(14, 2)
  total                Decimal             @default(0) @db.Decimal(14, 2)
  issueDate            DateTime            @default(now())
  expectedDeliveryDate DateTime?
  deliveredAt          DateTime?
  approvedAt           DateTime?
  notes                String?
  metadata             Json?
  createdAt            DateTime            @default(now())
  updatedAt            DateTime            @updatedAt
  deletedAt            DateTime?
  organisation         Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  supplier             Company?            @relation(fields: [supplierId], references: [id], onDelete: SetNull)
  project              Project?            @relation(fields: [projectId], references: [id], onDelete: SetNull)
  items                PurchaseOrderItem[]

  @@unique([organisationId, orderNumber])
  @@index([organisationId, supplierId])
  @@index([organisationId, projectId])
  @@index([organisationId, status])
  @@index([organisationId, issueDate])
  @@index([organisationId, deletedAt])
}

model PurchaseOrderItem {
  id              String        @id @default(uuid()) @db.Uuid
  organisationId  String        @db.Uuid
  purchaseOrderId String        @db.Uuid
  description     String
  quantity        Decimal       @default(1) @db.Decimal(12, 2)
  unitPrice       Decimal       @default(0) @db.Decimal(14, 2)
  taxRate         Decimal       @default(0) @db.Decimal(6, 3)
  total           Decimal       @default(0) @db.Decimal(14, 2)
  position        Int           @default(0)
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  deletedAt       DateTime?
  organisation    Organisation  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  purchaseOrder   PurchaseOrder @relation(fields: [purchaseOrderId], references: [id], onDelete: Cascade)

  @@index([organisationId, purchaseOrderId])
  @@index([organisationId, deletedAt])
}

model RecurringInvoice {
  id             String                    @id @default(uuid()) @db.Uuid
  organisationId String                    @db.Uuid
  invoiceId      String?                   @db.Uuid
  name           String
  frequency      RecurringBillingFrequency
  enabled        Boolean                   @default(true)
  nextRunAt      DateTime?
  lastRunAt      DateTime?
  timezone       String                    @default("UTC")
  metadata       Json?
  createdAt      DateTime                  @default(now())
  updatedAt      DateTime                  @updatedAt
  deletedAt      DateTime?
  organisation   Organisation              @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  invoice        Invoice?                  @relation(fields: [invoiceId], references: [id], onDelete: SetNull)

  @@index([organisationId, invoiceId])
  @@index([organisationId, frequency])
  @@index([organisationId, nextRunAt])
  @@index([organisationId, enabled])
  @@index([organisationId, deletedAt])
}

model Subscription {
  id              String                    @id @default(uuid()) @db.Uuid
  organisationId  String                    @db.Uuid
  customerId      String?                   @db.Uuid
  name            String
  status          SubscriptionStatus        @default(ACTIVE)
  currency        String                    @default("USD")
  amount          Decimal                   @default(0) @db.Decimal(14, 2)
  interval        RecurringBillingFrequency @default(MONTHLY)
  startDate       DateTime                  @default(now())
  endDate         DateTime?
  nextBillingDate DateTime?
  metadata        Json?
  createdAt       DateTime                  @default(now())
  updatedAt       DateTime                  @updatedAt
  deletedAt       DateTime?
  organisation    Organisation              @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  customer        Company?                  @relation(fields: [customerId], references: [id], onDelete: SetNull)

  @@index([organisationId, customerId])
  @@index([organisationId, status])
  @@index([organisationId, nextBillingDate])
  @@index([organisationId, deletedAt])
}

model Budget {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  projectId      String?      @db.Uuid
  name           String
  department     String?
  currency       String       @default("USD")
  amount         Decimal      @default(0) @db.Decimal(14, 2)
  spent          Decimal      @default(0) @db.Decimal(14, 2)
  periodStart    DateTime
  periodEnd      DateTime
  metadata       Json?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  project        Project?     @relation(fields: [projectId], references: [id], onDelete: SetNull)

  @@index([organisationId, projectId])
  @@index([organisationId, department])
  @@index([organisationId, periodStart, periodEnd])
  @@index([organisationId, deletedAt])
}

model Transaction {
  id             String          @id @default(uuid()) @db.Uuid
  organisationId String          @db.Uuid
  accountId      String?         @db.Uuid
  customerId     String?         @db.Uuid
  type           TransactionType
  currency       String          @default("USD")
  amount         Decimal         @default(0) @db.Decimal(14, 2)
  occurredAt     DateTime        @default(now())
  description    String?
  reference      String?
  metadata       Json?
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
  deletedAt      DateTime?
  organisation   Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  account        Account?        @relation(fields: [accountId], references: [id], onDelete: SetNull)
  customer       Company?        @relation(fields: [customerId], references: [id], onDelete: SetNull)

  @@index([organisationId, accountId])
  @@index([organisationId, customerId])
  @@index([organisationId, type])
  @@index([organisationId, occurredAt])
  @@index([organisationId, deletedAt])
}

model Account {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String        @db.Uuid
  name           String
  type           AccountType
  currency       String        @default("USD")
  balance        Decimal       @default(0) @db.Decimal(14, 2)
  institution    String?
  lastFour       String?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  deletedAt      DateTime?
  organisation   Organisation  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  transactions   Transaction[]

  @@unique([organisationId, name])
  @@index([organisationId, type])
  @@index([organisationId, deletedAt])
}

model TaxRate {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  name           String
  rate           Decimal      @default(0) @db.Decimal(6, 3)
  country        String?
  region         String?
  active         Boolean      @default(true)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([organisationId, name])
  @@index([organisationId, active])
  @@index([organisationId, deletedAt])
}

model Currency {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  code           String
  name           String
  symbol         String
  exchangeRate   Decimal      @default(1) @db.Decimal(14, 6)
  active         Boolean      @default(true)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([organisationId, code])
  @@index([organisationId, active])
  @@index([organisationId, deletedAt])
}

model FinancialReport {
  id             String              @id @default(uuid()) @db.Uuid
  organisationId String              @db.Uuid
  type           FinancialReportType
  name           String
  periodStart    DateTime
  periodEnd      DateTime
  data           Json
  generatedById  String?             @db.Uuid
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  deletedAt      DateTime?
  organisation   Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, type])
  @@index([organisationId, periodStart, periodEnd])
  @@index([organisationId, createdAt])
  @@index([organisationId, deletedAt])
}

model CashFlowEntry {
  id             String            @id @default(uuid()) @db.Uuid
  organisationId String            @db.Uuid
  customerId     String?           @db.Uuid
  projectId      String?           @db.Uuid
  direction      CashFlowDirection
  currency       String            @default("USD")
  amount         Decimal           @default(0) @db.Decimal(14, 2)
  expectedDate   DateTime
  actualDate     DateTime?
  description    String?
  sourceType     String?
  sourceId       String?           @db.Uuid
  createdAt      DateTime          @default(now())
  updatedAt      DateTime          @updatedAt
  deletedAt      DateTime?
  organisation   Organisation      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  customer       Company?          @relation(fields: [customerId], references: [id], onDelete: SetNull)
  project        Project?          @relation(fields: [projectId], references: [id], onDelete: SetNull)

  @@index([organisationId, customerId])
  @@index([organisationId, projectId])
  @@index([organisationId, direction])
  @@index([organisationId, expectedDate])
  @@index([organisationId, deletedAt])
}

model JournalEntry {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  entryNumber    String
  description    String?
  entryDate      DateTime     @default(now())
  lines          Json
  posted         Boolean      @default(false)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([organisationId, entryNumber])
  @@index([organisationId, entryDate])
  @@index([organisationId, posted])
  @@index([organisationId, deletedAt])
}

model FinanceAudit {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  actorUserId    String?      @db.Uuid
  action         String
  entityType     String
  entityId       String?      @db.Uuid
  metadata       Json?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  actorUser      User?        @relation("FinanceAuditActor", fields: [actorUserId], references: [id], onDelete: SetNull)

  @@index([organisationId, actorUserId])
  @@index([organisationId, entityType, entityId])
  @@index([organisationId, action])
  @@index([organisationId, createdAt])
}

model Dashboard {
  id             String              @id @default(uuid()) @db.Uuid
  organisationId String              @db.Uuid
  name           String
  description    String?
  visibility     DashboardVisibility @default(ORGANISATION)
  layout         Json?
  isDefault      Boolean             @default(false)
  createdById    String?             @db.Uuid
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  deletedAt      DateTime?
  organisation   Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  widgets        DashboardWidget[]
  savedViews     SavedView[]

  @@index([organisationId, visibility])
  @@index([organisationId, isDefault])
  @@index([organisationId, createdById])
  @@index([organisationId, deletedAt])
}

model DashboardWidget {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  dashboardId    String       @db.Uuid
  title          String
  type           WidgetType
  config         Json
  position       Json?
  dataSourceId   String?      @db.Uuid
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  dashboard      Dashboard    @relation(fields: [dashboardId], references: [id], onDelete: Cascade)
  dataSource     DataSource?  @relation(fields: [dataSourceId], references: [id], onDelete: SetNull)

  @@index([organisationId, dashboardId])
  @@index([organisationId, type])
  @@index([organisationId, dataSourceId])
  @@index([organisationId, deletedAt])
}

model Metric {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String        @db.Uuid
  key            String
  name           String
  type           MetricType
  description    String?
  unit           String?
  source         String?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  deletedAt      DateTime?
  organisation   Organisation  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  values         MetricValue[]
  kpis           KPI[]

  @@unique([organisationId, key])
  @@index([organisationId, type])
  @@index([organisationId, deletedAt])
}

model MetricValue {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  metricId       String       @db.Uuid
  value          Decimal      @db.Decimal(18, 4)
  periodStart    DateTime
  periodEnd      DateTime
  dimensions     Json?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  metric         Metric       @relation(fields: [metricId], references: [id], onDelete: Cascade)

  @@index([organisationId, metricId])
  @@index([organisationId, periodStart, periodEnd])
  @@index([organisationId, deletedAt])
}

model KPI {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  metricId       String?      @db.Uuid
  name           String
  target         Decimal?     @db.Decimal(18, 4)
  currentValue   Decimal?     @db.Decimal(18, 4)
  unit           String?
  status         String       @default("TRACKING")
  ownerUserId    String?      @db.Uuid
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  metric         Metric?      @relation(fields: [metricId], references: [id], onDelete: SetNull)

  @@index([organisationId, metricId])
  @@index([organisationId, status])
  @@index([organisationId, ownerUserId])
  @@index([organisationId, deletedAt])
}

model Report {
  id             String              @id @default(uuid()) @db.Uuid
  organisationId String              @db.Uuid
  name           String
  type           AnalyticsReportType
  description    String?
  filters        Json?
  configuration  Json?
  lastRunAt      DateTime?
  createdById    String?             @db.Uuid
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  deletedAt      DateTime?
  organisation   Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  schedules      ReportSchedule[]

  @@index([organisationId, type])
  @@index([organisationId, createdById])
  @@index([organisationId, deletedAt])
}

model ReportSchedule {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  reportId       String       @db.Uuid
  cronExpression String
  timezone       String       @default("UTC")
  recipients     Json?
  enabled        Boolean      @default(true)
  nextRunAt      DateTime?
  lastRunAt      DateTime?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  report         Report       @relation(fields: [reportId], references: [id], onDelete: Cascade)

  @@index([organisationId, reportId])
  @@index([organisationId, enabled])
  @@index([organisationId, nextRunAt])
  @@index([organisationId, deletedAt])
}

model DataSource {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  name           String
  module         String
  entity         String
  configuration  Json?
  active         Boolean              @default(true)
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  widgets        DashboardWidget[]
  charts         ChartConfiguration[]

  @@index([organisationId, module])
  @@index([organisationId, entity])
  @@index([organisationId, active])
  @@index([organisationId, deletedAt])
}

model ChartConfiguration {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  dataSourceId   String?      @db.Uuid
  name           String
  type           ChartType
  config         Json
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  dataSource     DataSource?  @relation(fields: [dataSourceId], references: [id], onDelete: SetNull)

  @@index([organisationId, type])
  @@index([organisationId, dataSourceId])
  @@index([organisationId, deletedAt])
}

model Forecast {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  type           ForecastType
  name           String
  horizonDays    Int          @default(90)
  inputSummary   Json?
  result         Json
  confidence     Decimal?     @db.Decimal(5, 4)
  generatedAt    DateTime     @default(now())
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, type])
  @@index([organisationId, generatedAt])
  @@index([organisationId, deletedAt])
}

model BusinessSnapshot {
  id              String       @id @default(uuid()) @db.Uuid
  organisationId  String       @db.Uuid
  snapshotDate    DateTime
  revenue         Decimal      @default(0) @db.Decimal(18, 2)
  projects        Int          @default(0)
  customers       Int          @default(0)
  employees       Int          @default(0)
  tasks           Int          @default(0)
  documents       Int          @default(0)
  automations     Int          @default(0)
  financialHealth Decimal?     @db.Decimal(5, 2)
  data            Json?
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt
  deletedAt       DateTime?
  organisation    Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([organisationId, snapshotDate])
  @@index([organisationId, snapshotDate])
  @@index([organisationId, deletedAt])
}

model AnalyticsEvent {
  id             String             @id @default(uuid()) @db.Uuid
  organisationId String             @db.Uuid
  actorUserId    String?            @db.Uuid
  type           AnalyticsEventType
  entityType     String?
  entityId       String?            @db.Uuid
  metadata       Json?
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt
  deletedAt      DateTime?
  organisation   Organisation       @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, actorUserId])
  @@index([organisationId, type])
  @@index([organisationId, entityType, entityId])
  @@index([organisationId, createdAt])
}

model AnalyticsFilter {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  name           String
  scope          String
  config         Json
  createdById    String?      @db.Uuid
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, scope])
  @@index([organisationId, createdById])
  @@index([organisationId, deletedAt])
}

model SavedView {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  dashboardId    String?      @db.Uuid
  name           String
  config         Json
  shared         Boolean      @default(false)
  createdById    String?      @db.Uuid
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  dashboard      Dashboard?   @relation(fields: [dashboardId], references: [id], onDelete: SetNull)

  @@index([organisationId, dashboardId])
  @@index([organisationId, shared])
  @@index([organisationId, createdById])
  @@index([organisationId, deletedAt])
}

model IntegrationType {
  id             String                @id @default(uuid()) @db.Uuid
  organisationId String                @db.Uuid
  key            String
  name           String
  category       IntegrationCategory
  authTypes      IntegrationAuthType[]
  description    String?
  metadata       Json?
  createdAt      DateTime              @default(now())
  updatedAt      DateTime              @updatedAt
  deletedAt      DateTime?
  organisation   Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integrations   Integration[]

  @@unique([organisationId, key])
  @@index([organisationId, category])
  @@index([organisationId, deletedAt])
}

model Connector {
  id             String                @id @default(uuid()) @db.Uuid
  organisationId String                @db.Uuid
  key            String
  name           String
  category       IntegrationCategory
  provider       String
  version        String                @default("0.1.0")
  developer      String                @default("mikyasOS")
  verified       Boolean               @default(false)
  featured       Boolean               @default(false)
  authTypes      IntegrationAuthType[]
  capabilities   Json?
  documentation  String?
  status         IntegrationStatus     @default(AVAILABLE)
  createdAt      DateTime              @default(now())
  updatedAt      DateTime              @updatedAt
  deletedAt      DateTime?
  organisation   Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integrations   Integration[]
  actions        ConnectorAction[]

  @@unique([organisationId, key])
  @@index([organisationId, category])
  @@index([organisationId, verified, featured])
  @@index([organisationId, status])
  @@index([organisationId, deletedAt])
}

model ConnectorAction {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  connectorId    String       @db.Uuid
  key            String
  name           String
  description    String?
  inputSchema    Json?
  outputSchema   Json?
  enabled        Boolean      @default(true)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  connector      Connector    @relation(fields: [connectorId], references: [id], onDelete: Cascade)

  @@unique([connectorId, key])
  @@index([organisationId, connectorId])
  @@index([organisationId, enabled])
  @@index([organisationId, deletedAt])
}

model Integration {
  id                String                  @id @default(uuid()) @db.Uuid
  organisationId    String                  @db.Uuid
  integrationTypeId String?                 @db.Uuid
  connectorId       String?                 @db.Uuid
  key               String
  name              String
  category          IntegrationCategory
  provider          String
  status            IntegrationStatus       @default(INSTALLED)
  config            Json?
  createdById       String?                 @db.Uuid
  createdAt         DateTime                @default(now())
  updatedAt         DateTime                @updatedAt
  deletedAt         DateTime?
  organisation      Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integrationType   IntegrationType?        @relation(fields: [integrationTypeId], references: [id], onDelete: SetNull)
  connector         Connector?              @relation(fields: [connectorId], references: [id], onDelete: SetNull)
  credentials       IntegrationCredential[]
  connections       IntegrationConnection[]
  syncs             IntegrationSync[]
  webhooks          WebhookEndpoint[]
  deliveries        WebhookDelivery[]
  logs              SyncLog[]
  health            IntegrationHealth[]
  events            IntegrationEvent[]
  apiKeys           APIKey[]

  @@unique([organisationId, key])
  @@index([organisationId, category])
  @@index([organisationId, provider])
  @@index([organisationId, status])
  @@index([organisationId, deletedAt])
}

model IntegrationCredential {
  id             String                  @id @default(uuid()) @db.Uuid
  organisationId String                  @db.Uuid
  integrationId  String                  @db.Uuid
  authType       IntegrationAuthType
  label          String
  encryptedData  Json
  fingerprint    String?
  expiresAt      DateTime?
  rotatedAt      DateTime?
  createdById    String?                 @db.Uuid
  createdAt      DateTime                @default(now())
  updatedAt      DateTime                @updatedAt
  deletedAt      DateTime?
  organisation   Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integration    Integration             @relation(fields: [integrationId], references: [id], onDelete: Cascade)
  connections    IntegrationConnection[]

  @@index([organisationId, integrationId])
  @@index([organisationId, authType])
  @@index([organisationId, expiresAt])
  @@index([organisationId, deletedAt])
}

model IntegrationConnection {
  id             String                 @id @default(uuid()) @db.Uuid
  organisationId String                 @db.Uuid
  integrationId  String                 @db.Uuid
  credentialId   String?                @db.Uuid
  status         ConnectionStatus       @default(PENDING)
  scopes         String[]
  metadata       Json?
  connectedAt    DateTime?
  disconnectedAt DateTime?
  lastCheckedAt  DateTime?
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  deletedAt      DateTime?
  organisation   Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integration    Integration            @relation(fields: [integrationId], references: [id], onDelete: Cascade)
  credential     IntegrationCredential? @relation(fields: [credentialId], references: [id], onDelete: SetNull)
  syncs          IntegrationSync[]
  health         IntegrationHealth[]

  @@index([organisationId, integrationId])
  @@index([organisationId, credentialId])
  @@index([organisationId, status])
  @@index([organisationId, deletedAt])
}

model IntegrationSync {
  id             String                 @id @default(uuid()) @db.Uuid
  organisationId String                 @db.Uuid
  integrationId  String                 @db.Uuid
  connectionId   String?                @db.Uuid
  mode           SyncMode
  status         SyncStatus             @default(QUEUED)
  entity         String
  startedAt      DateTime?
  completedAt    DateTime?
  nextRunAt      DateTime?
  checkpoint     Json?
  conflicts      Json?
  summary        Json?
  error          String?
  retryCount     Int                    @default(0)
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  deletedAt      DateTime?
  organisation   Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integration    Integration            @relation(fields: [integrationId], references: [id], onDelete: Cascade)
  connection     IntegrationConnection? @relation(fields: [connectionId], references: [id], onDelete: SetNull)
  logs           SyncLog[]

  @@index([organisationId, integrationId])
  @@index([organisationId, connectionId])
  @@index([organisationId, status])
  @@index([organisationId, nextRunAt])
  @@index([organisationId, deletedAt])
}

model WebhookEndpoint {
  id             String            @id @default(uuid()) @db.Uuid
  organisationId String            @db.Uuid
  integrationId  String?           @db.Uuid
  name           String
  direction      WebhookDirection
  url            String
  secretHash     String?
  events         String[]
  active         Boolean           @default(true)
  createdAt      DateTime          @default(now())
  updatedAt      DateTime          @updatedAt
  deletedAt      DateTime?
  organisation   Organisation      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integration    Integration?      @relation(fields: [integrationId], references: [id], onDelete: SetNull)
  deliveries     WebhookDelivery[]

  @@index([organisationId, integrationId])
  @@index([organisationId, direction, active])
  @@index([organisationId, deletedAt])
}

model WebhookDelivery {
  id             String                @id @default(uuid()) @db.Uuid
  organisationId String                @db.Uuid
  integrationId  String?               @db.Uuid
  endpointId     String?               @db.Uuid
  status         WebhookDeliveryStatus @default(PENDING)
  event          String
  payload        Json?
  responseStatus Int?
  responseBody   String?
  attempts       Int                   @default(0)
  nextAttemptAt  DateTime?
  deliveredAt    DateTime?
  createdAt      DateTime              @default(now())
  updatedAt      DateTime              @updatedAt
  deletedAt      DateTime?
  organisation   Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integration    Integration?          @relation(fields: [integrationId], references: [id], onDelete: SetNull)
  endpoint       WebhookEndpoint?      @relation(fields: [endpointId], references: [id], onDelete: SetNull)

  @@index([organisationId, integrationId])
  @@index([organisationId, endpointId])
  @@index([organisationId, status])
  @@index([organisationId, nextAttemptAt])
  @@index([organisationId, deletedAt])
}

model SyncLog {
  id             String                 @id @default(uuid()) @db.Uuid
  organisationId String                 @db.Uuid
  integrationId  String?                @db.Uuid
  syncId         String?                @db.Uuid
  severity       IntegrationLogSeverity @default(INFO)
  message        String
  metadata       Json?
  createdAt      DateTime               @default(now())
  updatedAt      DateTime               @updatedAt
  deletedAt      DateTime?
  organisation   Organisation           @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integration    Integration?           @relation(fields: [integrationId], references: [id], onDelete: SetNull)
  sync           IntegrationSync?       @relation(fields: [syncId], references: [id], onDelete: SetNull)

  @@index([organisationId, integrationId])
  @@index([organisationId, syncId])
  @@index([organisationId, severity])
  @@index([organisationId, createdAt])
  @@index([organisationId, deletedAt])
}

model IntegrationHealth {
  id             String                  @id @default(uuid()) @db.Uuid
  organisationId String                  @db.Uuid
  integrationId  String                  @db.Uuid
  connectionId   String?                 @db.Uuid
  status         IntegrationHealthStatus @default(UNKNOWN)
  latencyMs      Int?
  checkedAt      DateTime                @default(now())
  details        Json?
  createdAt      DateTime                @default(now())
  updatedAt      DateTime                @updatedAt
  deletedAt      DateTime?
  organisation   Organisation            @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integration    Integration             @relation(fields: [integrationId], references: [id], onDelete: Cascade)
  connection     IntegrationConnection?  @relation(fields: [connectionId], references: [id], onDelete: SetNull)

  @@index([organisationId, integrationId])
  @@index([organisationId, connectionId])
  @@index([organisationId, status])
  @@index([organisationId, checkedAt])
  @@index([organisationId, deletedAt])
}

model IntegrationEvent {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  integrationId  String?              @db.Uuid
  actorUserId    String?              @db.Uuid
  type           IntegrationEventType
  entityType     String?
  entityId       String?              @db.Uuid
  metadata       Json?
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integration    Integration?         @relation(fields: [integrationId], references: [id], onDelete: SetNull)

  @@index([organisationId, integrationId])
  @@index([organisationId, actorUserId])
  @@index([organisationId, type])
  @@index([organisationId, createdAt])
  @@index([organisationId, deletedAt])
}

model APIKey {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  integrationId  String?      @db.Uuid
  name           String
  keyHash        String
  prefix         String?
  scopes         String[]
  lastUsedAt     DateTime?
  expiresAt      DateTime?
  revokedAt      DateTime?
  createdById    String?      @db.Uuid
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  integration    Integration? @relation(fields: [integrationId], references: [id], onDelete: SetNull)

  @@index([organisationId, integrationId])
  @@index([organisationId, prefix])
  @@index([organisationId, expiresAt])
  @@index([organisationId, revokedAt])
  @@index([organisationId, deletedAt])
}

model BusinessUnit {
  id             String         @id @default(uuid()) @db.Uuid
  organisationId String         @db.Uuid
  parentId       String?        @db.Uuid
  name           String
  code           String?
  description    String?
  leaderUserId   String?        @db.Uuid
  settings       Json?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
  deletedAt      DateTime?
  organisation   Organisation   @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  parent         BusinessUnit?  @relation("BusinessUnitTree", fields: [parentId], references: [id], onDelete: SetNull)
  children       BusinessUnit[] @relation("BusinessUnitTree")

  @@unique([organisationId, code])
  @@index([organisationId, parentId])
  @@index([organisationId, leaderUserId])
  @@index([organisationId, deletedAt])
}

model OrganisationHierarchy {
  id                   String       @id @default(uuid()) @db.Uuid
  organisationId       String       @db.Uuid
  parentOrganisationId String       @db.Uuid
  childOrganisationId  String       @db.Uuid
  relationshipType     String       @default("CHILD")
  inheritedPermissions Boolean      @default(true)
  delegatedAdmin       Boolean      @default(false)
  metadata             Json?
  createdAt            DateTime     @default(now())
  updatedAt            DateTime     @updatedAt
  deletedAt            DateTime?
  organisation         Organisation @relation("HierarchyOwner", fields: [organisationId], references: [id], onDelete: Cascade)
  parentOrganisation   Organisation @relation("HierarchyParent", fields: [parentOrganisationId], references: [id], onDelete: Cascade)
  childOrganisation    Organisation @relation("HierarchyChild", fields: [childOrganisationId], references: [id], onDelete: Cascade)

  @@unique([parentOrganisationId, childOrganisationId])
  @@index([organisationId])
  @@index([parentOrganisationId])
  @@index([childOrganisationId])
  @@index([organisationId, deletedAt])
}

model EnterprisePolicy {
  id             String               @id @default(uuid()) @db.Uuid
  organisationId String               @db.Uuid
  type           EnterprisePolicyType
  name           String
  status         PolicyStatus         @default(DRAFT)
  version        Int                  @default(1)
  rules          Json
  effectiveFrom  DateTime?
  createdById    String?              @db.Uuid
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  deletedAt      DateTime?
  organisation   Organisation         @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, type])
  @@index([organisationId, status])
  @@index([organisationId, deletedAt])
}

model AuditEvent {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String        @db.Uuid
  actorUserId    String?       @db.Uuid
  severity       AuditSeverity @default(INFO)
  action         String
  module         String
  entityType     String?
  entityId       String?       @db.Uuid
  requestId      String?
  ipAddress      String?
  userAgent      String?
  metadata       Json?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  deletedAt      DateTime?
  organisation   Organisation  @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, actorUserId])
  @@index([organisationId, module, action])
  @@index([organisationId, severity])
  @@index([organisationId, createdAt])
  @@index([organisationId, deletedAt])
}

model SecurityPolicy {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  name           String
  status         PolicyStatus @default(DRAFT)
  mfaRequired    Boolean      @default(false)
  passwordRules  Json?
  ipAllowList    String[]
  trustedDevices Json?
  sessionRules   Json?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, status])
  @@index([organisationId, deletedAt])
}

model CustomRole {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  parentRoleId   String?      @db.Uuid
  name           String
  description    String?
  permissions    String[]
  temporaryRules Json?
  approvalRules  Json?
  delegatedAdmin Boolean      @default(false)
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  parentRole     CustomRole?  @relation("CustomRoleInheritance", fields: [parentRoleId], references: [id], onDelete: SetNull)
  childRoles     CustomRole[] @relation("CustomRoleInheritance")

  @@unique([organisationId, name])
  @@index([organisationId, parentRoleId])
  @@index([organisationId, delegatedAdmin])
  @@index([organisationId, deletedAt])
}

model PermissionGroup {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  name           String
  description    String?
  permissions    String[]
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([organisationId, name])
  @@index([organisationId, deletedAt])
}

model SSOProvider {
  id             String          @id @default(uuid()) @db.Uuid
  organisationId String          @db.Uuid
  type           SSOProviderType
  name           String
  enabled        Boolean         @default(false)
  domains        String[]
  metadata       Json?
  config         Json?
  lastTestedAt   DateTime?
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
  deletedAt      DateTime?
  organisation   Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, type])
  @@index([organisationId, enabled])
  @@index([organisationId, deletedAt])
}

model DirectoryConnection {
  id             String                @id @default(uuid()) @db.Uuid
  organisationId String                @db.Uuid
  type           DirectoryProviderType
  name           String
  enabled        Boolean               @default(false)
  provisioning   Boolean               @default(false)
  deprovisioning Boolean               @default(false)
  config         Json?
  lastSyncAt     DateTime?
  nextSyncAt     DateTime?
  createdAt      DateTime              @default(now())
  updatedAt      DateTime              @updatedAt
  deletedAt      DateTime?
  organisation   Organisation          @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, type])
  @@index([organisationId, enabled])
  @@index([organisationId, nextSyncAt])
  @@index([organisationId, deletedAt])
}

model ComplianceRecord {
  id             String              @id @default(uuid()) @db.Uuid
  organisationId String              @db.Uuid
  framework      ComplianceFramework
  status         ComplianceStatus    @default(IN_PROGRESS)
  control        String
  evidence       Json?
  ownerUserId    String?             @db.Uuid
  reviewedAt     DateTime?
  dueAt          DateTime?
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  deletedAt      DateTime?
  organisation   Organisation        @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, framework])
  @@index([organisationId, status])
  @@index([organisationId, dueAt])
  @@index([organisationId, deletedAt])
}

model RetentionPolicy {
  id              String          @id @default(uuid()) @db.Uuid
  organisationId  String          @db.Uuid
  name            String
  entityType      String
  action          RetentionAction
  durationDays    Int?
  enabled         Boolean         @default(true)
  legalHoldBypass Boolean         @default(false)
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt
  deletedAt       DateTime?
  organisation    Organisation    @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, entityType])
  @@index([organisationId, enabled])
  @@index([organisationId, deletedAt])
}

model LegalHold {
  id             String       @id @default(uuid()) @db.Uuid
  organisationId String       @db.Uuid
  name           String
  reason         String
  entityType     String?
  entityId       String?      @db.Uuid
  active         Boolean      @default(true)
  startsAt       DateTime     @default(now())
  endsAt         DateTime?
  createdById    String?      @db.Uuid
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  deletedAt      DateTime?
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, active])
  @@index([organisationId, entityType, entityId])
  @@index([organisationId, deletedAt])
}

model License {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String        @db.Uuid
  plan           String
  status         LicenseStatus @default(TRIAL)
  seats          Int           @default(1)
  usedSeats      Int           @default(0)
  startsAt       DateTime      @default(now())
  expiresAt      DateTime?
  features       String[]
  metadata       Json?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  deletedAt      DateTime?
  organisation   Organisation  @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, status])
  @@index([organisationId, expiresAt])
  @@index([organisationId, deletedAt])
}

model PlatformHealthCheck {
  id             String         @id @default(uuid()) @db.Uuid
  organisationId String?        @db.Uuid
  service        String
  status         PlatformStatus @default(OPERATIONAL)
  responseTimeMs Int?
  details        Json?
  checkedAt      DateTime       @default(now())
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
  deletedAt      DateTime?
  organisation   Organisation?  @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, service])
  @@index([organisationId, status])
  @@index([checkedAt])
  @@index([deletedAt])
}

model PlatformMetric {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String?       @db.Uuid
  name           String
  value          Decimal       @db.Decimal(18, 4)
  unit           String?
  dimensions     Json?
  recordedAt     DateTime      @default(now())
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  deletedAt      DateTime?
  organisation   Organisation? @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, name])
  @@index([recordedAt])
  @@index([deletedAt])
}

model Incident {
  id               String             @id @default(uuid()) @db.Uuid
  organisationId   String?            @db.Uuid
  title            String
  description      String?
  severity         IncidentSeverity
  status           IncidentStatus     @default(INVESTIGATING)
  affectedServices String[]
  startedAt        DateTime           @default(now())
  detectedAt       DateTime           @default(now())
  resolvedAt       DateTime?
  ownerUserId      String?            @db.Uuid
  rootCause        String?
  customerImpact   String?
  resolution       String?
  followUps        Json?
  createdAt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt
  deletedAt        DateTime?
  organisation     Organisation?      @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  timeline         IncidentTimeline[]

  @@index([organisationId, severity])
  @@index([organisationId, status])
  @@index([startedAt])
  @@index([deletedAt])
}

model IncidentTimeline {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String?       @db.Uuid
  incidentId     String        @db.Uuid
  actorUserId    String?       @db.Uuid
  eventType      String
  message        String
  metadata       Json?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  deletedAt      DateTime?
  organisation   Organisation? @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  incident       Incident      @relation(fields: [incidentId], references: [id], onDelete: Cascade)

  @@index([organisationId, incidentId])
  @@index([incidentId, createdAt])
  @@index([deletedAt])
}

model DeploymentRecord {
  id                String           @id @default(uuid()) @db.Uuid
  organisationId    String?          @db.Uuid
  version           String
  commitSha         String?
  environment       String
  initiatedBy       String?
  status            DeploymentStatus @default(PENDING)
  migrationResult   Json?
  healthCheckResult Json?
  rollbackStatus    String?
  startedAt         DateTime         @default(now())
  completedAt       DateTime?
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  deletedAt         DateTime?
  organisation      Organisation?    @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, environment])
  @@index([status])
  @@index([startedAt])
  @@index([deletedAt])
}

model FeatureFlag {
  id             String           @id @default(uuid()) @db.Uuid
  organisationId String?          @db.Uuid
  key            String
  name           String
  scope          FeatureFlagScope @default(GLOBAL)
  enabled        Boolean          @default(false)
  rolloutPercent Int              @default(0)
  rules          Json?
  killSwitch     Boolean          @default(false)
  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt
  deletedAt      DateTime?
  organisation   Organisation?    @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([organisationId, key])
  @@index([scope, enabled])
  @@index([organisationId, deletedAt])
}

model BackupRecord {
  id                 String        @id @default(uuid()) @db.Uuid
  organisationId     String?       @db.Uuid
  name               String
  status             BackupStatus  @default(SCHEDULED)
  target             String
  sizeBytes          BigInt?
  encrypted          Boolean       @default(true)
  retentionDays      Int?
  verificationResult Json?
  restoreTestedAt    DateTime?
  startedAt          DateTime?
  completedAt        DateTime?
  createdAt          DateTime      @default(now())
  updatedAt          DateTime      @updatedAt
  deletedAt          DateTime?
  organisation       Organisation? @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, status])
  @@index([target])
  @@index([createdAt])
  @@index([deletedAt])
}

model RecoveryAction {
  id             String           @id @default(uuid()) @db.Uuid
  organisationId String?          @db.Uuid
  action         String
  reason         String
  safe           Boolean          @default(true)
  status         DeploymentStatus @default(PENDING)
  verification   Json?
  createdAt      DateTime         @default(now())
  updatedAt      DateTime         @updatedAt
  deletedAt      DateTime?
  organisation   Organisation?    @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, action])
  @@index([status])
  @@index([deletedAt])
}

model CircuitBreaker {
  id             String              @id @default(uuid()) @db.Uuid
  organisationId String?             @db.Uuid
  service        String
  state          CircuitBreakerState @default(CLOSED)
  failureCount   Int                 @default(0)
  lastFailureAt  DateTime?
  openedAt       DateTime?
  halfOpenedAt   DateTime?
  metadata       Json?
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  deletedAt      DateTime?
  organisation   Organisation?       @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([organisationId, service])
  @@index([state])
  @@index([deletedAt])
}

model JobQueueSnapshot {
  id                String        @id @default(uuid()) @db.Uuid
  organisationId    String?       @db.Uuid
  queueName         String
  waiting           Int           @default(0)
  active            Int           @default(0)
  completed         Int           @default(0)
  failed            Int           @default(0)
  delayed           Int           @default(0)
  deadLettered      Int           @default(0)
  averageDurationMs Int?
  failureRate       Decimal?      @db.Decimal(6, 4)
  recordedAt        DateTime      @default(now())
  createdAt         DateTime      @default(now())
  updatedAt         DateTime      @updatedAt
  deletedAt         DateTime?
  organisation      Organisation? @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, queueName])
  @@index([recordedAt])
  @@index([deletedAt])
}

model CostRecord {
  id             String        @id @default(uuid()) @db.Uuid
  organisationId String?       @db.Uuid
  category       String
  provider       String?
  amount         Decimal       @db.Decimal(18, 4)
  currency       String        @default("USD")
  usage          Json?
  recordedAt     DateTime      @default(now())
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  deletedAt      DateTime?
  organisation   Organisation? @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@index([organisationId, category])
  @@index([recordedAt])
  @@index([deletedAt])
}
